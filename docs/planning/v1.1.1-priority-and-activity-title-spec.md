# V1.1.1 우선순위 및 활동·칭호 대시보드 정책 v1

최신 기준일: 2026-07-11

이 문서는 V1.1 final sign-off 이후 V1.1.1 후보 기능의 우선순위와 1순위 구현 범위를 고정한다.
초기 구현 대상은 `고급 XP/칭호/훈장/활동내역 대시보드 1차 MVP`였고, 2026-07-09 고도화 1차에서 privacy-limited `누리 랭킹`, Lv.1~30, 장기 summary RPC, 운영자 알림 기반까지 확장했다. 2026-07-10에는 별도 `admin-console` 운영자 알림 관리 UI와 새 알림 live retention visual smoke를 닫았다. 2026-07-11 pre-store polish에서는 성장 시스템을 Lv.100까지 확장하고, 메인 홈에 현재 펫 기준 대표 칭호 badge를 추가했다. 이어서 Home/domain loading polish로 대표 칭호 cache, 활동·칭호 skeleton, 랭킹 탭별 cache/skeleton을 적용했다. 실서비스급 안정화에서는 기록/일정 disk cache, activity long summary RPC 재사용, ranking React Query 전환, 타임라인/커뮤니티 지연 로딩을 추가했다. 공개 경쟁형 리더보드와 push 실제 발송은 아직 제외한다.

## 1. V1.1.1 후보 우선순위

| 우선순위 | 기능 | 이번 턴 처리 | 구현 여부 | 이유 | 후속 필요 조건 |
| --- | --- | --- | --- | --- | --- |
| 1 | 고급 XP/칭호/훈장/활동내역 | 구현 | 예 | XP ledger, level summary, title MVP가 이미 존재해 사용자 리텐션과 성취감에 바로 연결된다. | Android final smoke, 장기 운영 지표 |
| 2 | 운영자 알림 발송 관리 체계 | 1차 UI 구현 | 예 | 새 알림 row live smoke와 push의 선행 조건이며 audit/RLS와 별도 관리 콘솔 1차가 필요했다. | 관리 페이지 고도화, 발송 승인/취소, push opt-in |
| 3 | push 알림 | 범위 확정 | 아니오 | 재방문에 중요하지만 permission, opt-out, token 저장, delivery log, 개인정보 고지가 먼저 필요하다. | 운영자 발송 관리 UI 이후 FCM/permission/token/RLS 설계 |
| 4 | 휴대폰 실기기 홈 위젯 | 범위 확정 | 아니오 | Android native surface이므로 개인정보 노출, update interval, 배터리, snapshot 계약 재설계가 필요하다. | Android AppWidget 재설계, release receiver 재활성 기준 |
| 5 | 무지개다리 서비스 | 범위 확정 | 아니오 | 감정적으로 민감해 문구, 노출 빈도, 문의/상품/결제 정책을 조심스럽게 설계해야 한다. | UX copy review, one-time suggestion, 상품/문의 정책 |
| 6 | 고급 공개 랭킹/리더보드 | 제한 구현 | 부분 | 이번 턴은 privacy-limited `누리 랭킹`만 구현했다. 공개 경쟁/opt-in 리더보드는 별도 설계가 필요하다. | opt-in, 익명화 정책, abuse 방어 |

## 2. 기존 XP/level/title 구조 분석

현재 구현된 서버 계약은 V1.1 2차 MVP 기준이다.

| 항목 | 현재 상태 |
| --- | --- |
| XP ledger | `user_xp_ledger`, `user_id`, optional `pet_id`, `event_type`, `source_type`, `source_id`, `xp`, KST date, unique source key |
| level summary | `user_level_summaries`, user-level total XP, `level between 1 and 100` |
| title storage | `user_titles`, user-level unique `title_key`, optional `pet_id`는 존재하지만 unique key는 user+title 기준 |
| XP award RPC | `award_user_activity_xp_v1`, source idempotency, daily cap, base activity cap |
| summary RPC | `get_user_level_summary_v1`, `get_user_titles_v1` |
| daily streak | `pet_daily_activities`, `pet_streak_summaries`, `get_pet_daily_status_v1` |
| 현재 실제 연결 | 산책 타임라인, 일반 타임라인, 건강 기록, 커뮤니티 글, 댓글 |
| user/pet isolation | RLS 기준 자기 row만 select. dashboard도 cross-user/ranking query 없음 |

레벨은 2026-07-11 기준 Lv.1~100으로 확장됐다. 기존 Lv.1~30 threshold는 유지해 기존 사용자 레벨 하락을 막고, Lv.31부터 Lv.100까지는 후반으로 갈수록 요구 XP가 커지는 curve를 사용한다. Lv.100 max threshold는 `1,250,000 XP`이며, Lv.100 이후는 무한 레벨이 아니라 `최고 레벨 달성`으로 표시한다.

## 2-1. 2026-07-09 고도화 1차 구현 결과

- migration: `20260709120000_v111_rankings_notifications_level30.sql`
- 운영자 알림 기반: `admin_notification_campaigns`, `admin_notification_audit_logs`, `admin_send_user_notification_v1`
- QA 알림 live smoke 기반: `create_qa_user_notification_v1`, `QA_RETENTION_` prefix, user/day 5건 제한, service role key 불필요
- 장기 요약: `get_user_activity_long_summary_v1`
- 랭킹: `get_activity_ranking_v1`, 전체메뉴 `누리 랭킹`, 종합/산책/글/댓글/건강/생활/미용 탭
- privacy: ranking RPC는 email, phone, user_id, pet_id, raw id를 반환하지 않고 pending deletion 계정을 제외한다.
- QA fixture: `activity_ranking_qa_fixtures`에 `adminQA3`~`adminQA8` 6개 deterministic row를 둔다. QA fixture caller가 명시 요청할 때만 포함된다.

## 2-2. 2026-07-10 운영자 알림 UI / live retention / 랭킹 visual closeout

- admin console: `admin-console/notification-console.html`
- policy helper: `src/services/admin/adminNotificationConsolePolicy.ts`
- focused test: `__tests__/adminNotificationConsolePolicy.test.ts`
- migration: `20260710090000_v111_admin_notification_console.sql`
- corrective migration: `20260710091500_v111_admin_notification_console_profile_guard_fix.sql`
- RPC: `admin_send_qa_user_notification_v1`, `admin_notification_audit_feed_v1`
- guard: anon admin RPC 401/42501 차단, non-admin admin send 42501 차단, broadcast disabled, 앱 내부 일반 사용자 UI 미노출
- live retention: 실제 admin wrapper 경로로 `adminQA` 단일 대상 알림 row 생성, 홈 표시, home dismiss, 알림함 유지, 알림함 개별 삭제, 전체삭제 user-scoped hide, 홈 재노출 없음 확인
- ranking visual: `누리 랭킹` 전체 탭 전환, 기둥그래프, 제한 필드 미노출 확인. 2026-07-11 Lv.100 정책 이후 fixture display는 `total_xp` 기준으로 재계산한다.
- PremiumRewardModal max visual: fixture auth 세션 부재 시 직접 fixture 로그인 visual은 focused max state test와 ranking visual로 보완한다. 2026-07-11 모달은 Lv.100/max progress bar와 `최고 레벨 달성` 문구를 표시한다.

## 2-3. 2026-07-11 Lv.100 / 홈 대표 칭호 / pre-store polish

- migration: `20260711110000_v111_level100_reward_decay.sql`
- 레벨 범위: Lv.1~100, Lv.100 max `1,250,000 XP`
- 호환성: 기존 Lv.1~30 threshold는 유지한다. Lv.31~100만 장기 curve로 추가한다.
- XP 기본 보상: 기존 대비 약 1.3배로 상향하되, 현재 레벨 구간별 감쇠 multiplier를 적용한다.
- 감쇠 구간: Lv.1~10 `100%`, Lv.11~30 `90%`, Lv.31~50 `80%`, Lv.51~70 `70%`, Lv.71~90 `60%`, Lv.91~100 `50%`
- 안전장치: 최소 지급 XP는 1 이상으로 clamp하고, daily cap/source idempotency는 기존 서버 계약을 유지한다.
- 홈 대표 칭호: `user_titles.pet_id`가 현재 선택/대표 펫과 일치하는 pet-level 칭호만 작은 badge로 표시한다. `pet_id=null`인 user-level 공통 칭호는 펫 칭호처럼 오표시하지 않는다.
- push token/opt-in, Play Store 자산, 홈 위젯, 무지개다리는 이번 polish에서 구현하지 않았다.

## 2-4. 2026-07-11 Home/domain loading polish

- Home: 기록/일정 bootstrap, 날씨 refresh, 추천 가이드 fetch, 대표 칭호 RPC를 첫 paint 이후로 분리한다.
- 대표 칭호: user/pet scoped AsyncStorage cache를 우선 표시하고 background Supabase refresh로 갱신한다. cache key는 user id와 pet id를 포함해 다른 사용자 또는 다른 pet 칭호가 섞이지 않게 한다.
- 활동·칭호: initial loading 때 전체 화면 spinner 대신 header/ScrollView shell과 skeleton card를 먼저 표시한다.
- 누리 랭킹: 탭별 row cache를 유지해 방문한 탭은 즉시 재표시하고, 새 탭은 skeleton을 보여 이전 탭 데이터 flicker를 막는다.
- DB/RPC/RLS/seed 변경 없이 앱 로딩 체감과 release readiness만 보강했다.

## 2-5. 2026-07-11 production readiness optimization

- 기록/일정 cache: `homeRecordScheduleCache`를 추가해 최근 기록/일정 preview를 userId+petId scoped AsyncStorage cache로 저장한다. schema version, generatedAt, 3일 TTL, corrupt cache fallback, logout/account transition clear를 포함한다.
- 활동 요약: `loadActivityDashboard`는 `get_user_activity_long_summary_v1`를 우선 호출해 user-level XP/community/comment summary를 재사용하고, 실패 시 기존 level/title/count query로 fallback한다. pet-scoped walk/timeline/health 분리는 유지한다.
- 랭킹: `NuriRankingScreen`은 React Query 기반으로 전환해 category+QA fixture flag query key, 60초 staleTime, 10분 gcTime, placeholderData를 사용한다. 탭 전환 중 기존 데이터는 유지하고 error/empty는 탭 내부에서 처리한다.
- 타임라인/커뮤니티: 타임라인 category count와 XP/title/status 보조 fetch, 커뮤니티 list 첫 fetch를 `InteractionManager` 이후로 지연해 화면 shell과 기존 list 렌더링을 먼저 확보한다.
- 보안: cache key는 user/pet scoped이고, ranking 화면은 기존 제한 필드 정책을 유지해 email, phone, user_id, pet_id, raw UUID를 노출하지 않는다.
- 제외: push token table, 실제 push 발송, Play Store 자산, 관리자 홈페이지 본구현, DB/RPC/RLS/seed 변경은 수행하지 않는다.

## 3. XP 지급 이벤트 정책

이번 턴에서 유지하거나 연결한 event type은 아래와 같다.

| 이벤트 | XP | daily limit | scope | 실제 연결 상태 |
| --- | ---: | ---: | --- | --- |
| 산책 기록 | base 26, 감쇠 적용 | 2 | pet | 정책/RPC 존재. 산책 장소 기록 경로 직접 연결은 후속 |
| 산책 타임라인 게시글 | base 39, 감쇠 적용 | 1 | pet | 연결 완료 |
| 일반 타임라인 게시글 | base 20, 감쇠 적용 | 3 | pet | 연결 완료 |
| 건강 기록 | base 13, 감쇠 적용 | 2 | pet | 건강 카테고리 기록 작성 시 `health_record`로 연결 |
| 커뮤니티 글 | base 13, 감쇠 적용 | 2 | user common | 작성 성공 후 user-level 후처리로 연결 |
| 댓글 | base 4, 감쇠 적용 | 10 | user common | 댓글 작성 성공 후 user-level 후처리로 연결 |
| 3/7/30일 streak bonus | base 39/104/390, 감쇠 적용 | 1 | pet/user event | 정책/RPC event type 존재. bonus 자동 지급 고도화는 후속 |

XP 지급 실패는 원본 작성 flow를 막지 않는다. 서버 RPC의 idempotency와 daily cap이 source of truth이며, 앱은 후처리 실패를 사용자 작성 실패로 전파하지 않는다.

## 4. 멀티펫 분리 정책

pet-scoped 활동은 pet 단위로 분리한다.

- 산책 기록
- 산책 streak
- pet_id가 있는 타임라인 XP
- 건강 기록
- pet_id가 있는 XP ledger
- pet별 조건으로 계산한 칭호/훈장

user-scoped 공통 활동은 특정 펫 하나에 귀속하지 않는다.

- 커뮤니티 글
- 댓글
- pet_id 없는 XP ledger
- user-level 대표 칭호

커뮤니티 글/댓글 XP는 `pet_id=null`로 기록하며, 멀티펫 카드에 중복 합산하지 않는다. 기존 데이터에 pet_id가 없는 활동은 임의 pet에 매핑하지 않는다.

## 5. UI 구현 기준

구현 위치는 `전체메뉴 > 나의 반려동물 > 활동·칭호`다.

탭/섹션 명칭은 `활동·칭호`로 확정한다. XP, 레벨, 칭호, 훈장을 모두 포괄하면서 `나의 활동내역`보다 딱딱하지 않고 반려동물 앱 톤에 맞기 때문이다.

1차 MVP 카드:

- 현재 성장 카드: 현재 레벨, 총 XP, 다음 레벨까지 남은 XP, progress bar, 대표 칭호
- 아이별 성장 기록: 멀티펫 분리 선택 카드
- 펫별 활동 카드: 산책, 타임라인, 건강관리
- 타임라인 카테고리 카드: 기존 카테고리 count read path 재사용
- 공통 활동 카드: 커뮤니티 글, 댓글
- 칭호·훈장 보관함: 획득/미획득 조건 표시

디자인 톤은 premium pet diary/activity archive로 유지한다. 경쟁형 ranking, leaderboard, 과한 게임 UI는 제외한다.

## 6. 보안/RLS 원칙

- 대시보드는 기존 user-scoped RLS table과 authenticated RPC만 사용한다.
- cross-user ranking query는 없다.
- user-level 공통 활동과 pet-level 활동을 UI와 view model에서 분리한다.
- 커뮤니티 활동을 모든 펫에 중복 합산하지 않는다.
- raw/internal/admin field와 secret/token은 표시하지 않는다.
- 신규 migration/RPC는 이번 턴에서 생성하지 않았다.

## 7. 1차 MVP 완료 조건

- V1.1.1 후보 우선순위 문서화
- 기존 Lv.1~10 서버 계약 확인
- `활동·칭호` route와 전체메뉴 entry 추가
- 성장 카드, 펫별 활동 카드, 공통 활동 카드, 칭호·훈장 보관함 구현
- 건강/커뮤니티/댓글 XP 후처리 연결
- multi-pet separation focused test 통과
- Android adminQA smoke 및 keyboard/nav bar QA 통과

## 8. Android / QA evidence

- Android 기기: `SM_S937N / R5CY613NMSY`
- release APK: `android/app/build/outputs/apk/release/app-release.apk`
- install/update: 성공
- cold start: 성공, `adminQA` 홈 세션 유지
- 전체메뉴 entry: `/tmp/nuri-qa/v111-more.png`
- 활동·칭호 상단/성장 카드: `/tmp/nuri-qa/v111-activity-title-dashboard-home.png`
- 펫별/타임라인/공통 활동 카드: `/tmp/nuri-qa/v111-activity-title-dashboard-pet-card.png`
- 칭호·훈장 보관함: `/tmp/nuri-qa/v111-activity-title-dashboard-achievements.png`
- uiautomator evidence: `/tmp/nuri-qa/v111-activity-title-dashboard.xml`, `/tmp/nuri-qa/v111-activity-title-dashboard-mid.xml`, `/tmp/nuri-qa/v111-activity-title-dashboard-achievements.xml`
- keyboard/nav bar: 입력창 없음으로 keyboard bar는 해당 없음. 하단 nav와 스크롤 콘텐츠 overlap 없음.
- logcat: fatal / ANR / unhandled promise / ReactNativeJS fatal pattern 0건.

## 9. 2026-07-03 closeout 갱신

### 활동·칭호 실제 데이터 QA

- `adminQA` 고정 일반 사용자 계정으로 산책 카테고리 타임라인 글 `ADMIN_QA_TEST`를 실제 저장했다.
- 최초 산책 write smoke에서는 저장 직후 데일리 streak 축하 모달이 표시됐고, 후속 조건부 closeout 해소 smoke에서는 AdminQAPet2 다중 작성 후 총 XP가 `120 XP`, AdminQAPet `60 XP · 훈장 2개`, AdminQAPet2 `45 XP · 훈장 1개`로 반영됐다.
- 타임라인 카테고리 카드는 `전체 1`, `산책 1`, `식사 0`, `일기장 0`, `생활 0`으로 표시됐다.
- 공통 활동 카드는 커뮤니티 글과 댓글을 사용자 전체 기준으로만 표시하며, AdminQAPet 카드에 중복 합산하지 않았다.
- 칭호·훈장 보관함은 `첫 산책 친구`, `첫 추억 기록`을 획득 상태로, `첫 건강 기록`, `첫 인사 완료`를 잠금 상태로 표시했다.
- 카테고리별 게시물 등록과 글/댓글 반영은 focused test로 추가 고정했다.
- Android evidence:
  - `/tmp/nuri-qa/v111-activity-title-closeout-after-write-overview.png`
  - `/tmp/nuri-qa/v111-activity-title-closeout-after-write-detail.png`
  - `/tmp/nuri-qa/v111-activity-title-closeout-after-write-achievements.png`

### 알림 보존 정책

- 홈 floating 알림창의 좌우 swipe dismiss와 `모두 치우기`는 이제 홈 간편창에서만 숨기는 local/user-scoped 상태로 분리한다.
- 전체보기/알림함의 개별 삭제와 전체삭제는 기존 서버 RPC의 user-scoped hide를 유지한다.
- read, home quick dismiss, inbox delete는 서로 다른 상태로 취급한다.
- 홈에서 치운 알림은 전체보기/알림함에는 남아야 하고, 전체보기에서 삭제한 알림은 홈 간편창에도 다시 나오면 안 된다.
- close, backdrop close, Android back close는 삭제도 home dismiss도 아니다.
- 이번 셸에는 service role key가 없어 adminQA 새 알림 row를 직접 생성하지 못했다. live item retention smoke는 focused test와 기존 read path 증적으로 보완하고, 운영/관리 도구가 준비된 뒤 실제 알림 row evidence를 추가한다.

### 보안/RLS

- anon direct select는 user-scoped table에서 row 0 또는 42501로 닫혔다.
- anon RPC는 `get_user_notifications_v1`, `dismiss_user_notification_v1`, `dismiss_all_user_notifications_v1`, `get_user_titles_v1`, `award_user_activity_xp_v1`, `record_pet_daily_activity_v1` 모두 42501로 차단됐다.
- 신규 DB/RPC/migration 없이 기존 RLS와 user/pet isolation을 유지했다.

## 10. 후속

- V1.1.1 1차 조건부 closeout 해소: 실제 멀티펫 edge QA, XP 다중 write smoke, 활동·칭호 owner label 보강
- V1.1.1 다음 후보: 운영자 발송 관리 체계와 push 알림의 선후 관계 확정
- 남은 Phase 2: streak bonus 자동 지급, ranking opt-in/abuse 방어, 운영자 관리 페이지 UI, push 실제 발송

## 11. 2026-07-03 조건부 closeout 해소 갱신

### 실제 멀티펫 edge QA

- `adminQA` 계정에 QA 전용 두 번째 pet `AdminQAPet2`를 추가했다. 새 랜덤 계정은 만들지 않았고, admin 권한도 부여하지 않았다.
- `AdminQAPet`과 `AdminQAPet2`가 활동·칭호 화면의 아이별 성장 기록 카드에 분리 표시되는 것을 Android에서 확인했다.
- `AdminQAPet2` 기준 산책/식사/일기장/생활 타임라인 글을 실제 저장했고, `AdminQAPet2 활동 요약`의 타임라인 카테고리 카드가 `전체 5`, `산책 2`, `식사 1`, `일기장 1`, `생활 1`로 반영됐다.
- 커뮤니티 글과 댓글은 공통 활동 카드에만 표시되며, pet별 카드에 중복 합산되지 않는 상태를 실기기와 focused test로 확인했다.
- 칭호·훈장 보관함에서 pet-level 동일 칭호명이 중복처럼 보이지 않도록 achievement에 `ownerId`/`ownerLabel`을 추가했다. 화면에는 `AdminQAPet · ...`, `AdminQAPet2 · ...`, `공통 활동 · ...`처럼 범위가 명확히 표시된다.

### XP 다중 write smoke

- 실제 작성: `AdminQAPet2` 기준 산책 2건, 식사 1건, 일기장 1건, 생활 1건을 `ADMIN_QA_TEST_*` prefix로 저장했다.
- 타임라인 카운트: Android timeline 화면에서 `전체 5`, `산책 2`, `식사 1`, `일기장 1`을 확인했다. 생활 카운트는 활동·칭호 카테고리 카드에서 `생활 1`로 확인했다.
- XP 반영: 활동·칭호 현재 성장 카드는 `Lv.2`, `120 XP`, 다음 레벨까지 `130 XP`를 표시했다.
- pet별 반영: `AdminQAPet`은 `60 XP · 훈장 2개`, `AdminQAPet2`는 `45 XP · 훈장 1개`로 표시됐다.
- 같은 날 산책 2건 작성 시 두 번째 작성은 timeline/streak에는 반영되지만 산책 XP daily cap 때문에 추가 산책 XP가 중복 지급되지 않았다.
- 산책 streak는 `오늘 산책 완료 · 1일 연속`으로 유지됐고, 두 번째 산책 글에서 축하 모달이 반복 노출되지 않았다.

### 알림 보존 정책 live evidence

- 현재 앱/DB 계약에는 authenticated 사용자가 자기 알림 row를 직접 생성하는 QA 전용 RPC가 없고, `user_notifications`는 select-only RLS다.
- service role key는 요구하지 않았고, 코드/로그/문서에 노출하지 않았다.
- 따라서 새 알림 row 기반 live retention smoke는 이번 턴에서도 안전하게 수행하지 않았다.
- 보장 범위는 `notificationRetentionPolicy` focused test, 기존 알림 read path evidence, home-only `AsyncStorage` dismiss service, server user-scoped inbox delete RPC 재검증으로 닫는다.
- 이 조건부 evidence gap은 운영자 발송 관리 체계 또는 QA fixture가 생기면 실제 row로 추가 확인한다. release blocker와 V1.1.1 blocker는 아니다.

### Android evidence

- cold start: `/tmp/nuri-qa/v111-conditional-closeout-cold-start.png`
- 홈 알림 empty/live-row 부재 확인: `/tmp/nuri-qa/v111-conditional-closeout-home-notification.png`
- 두 번째 pet 생성 성공: `/tmp/nuri-qa/v111-conditional-closeout-pet-create-success.png`
- 멀티펫 홈 표시: `/tmp/nuri-qa/v111-conditional-closeout-multipet-home.png`
- 다중 타임라인 작성 후 목록: `/tmp/nuri-qa/v111-conditional-closeout-timeline-after-multi-writes.png`
- 활동·칭호 성장 카드: `/tmp/nuri-qa/v111-conditional-closeout-activity-postfix-overview.png`
- 활동·칭호 카테고리/공통 활동: `/tmp/nuri-qa/v111-conditional-closeout-activity-postfix-scroll1.png`
- owner label이 반영된 칭호·훈장 보관함: `/tmp/nuri-qa/v111-conditional-closeout-activity-postfix-achievements.png`
- logcat: `/tmp/nuri-qa/v111-conditional-closeout-logcat.txt`, fatal / ANR / unhandled promise / ReactNativeJS fatal pattern 0건

## 12. 다음 V1.1.1 후보 우선순위

다음 후보 구현 순서는 “사용자 체감”보다 “운영 방어선과 안전한 발송 계약”을 먼저 둔다. 앱 내부 admin UI는 계속 금지하고, 운영자 기능은 홈페이지/관리 페이지 트랙으로 유지한다.

| 후보 | 추천 우선순위 | 이번 턴 구현 여부 | 선행 조건 | 위험도 | 추천 다음 액션 |
| --- | ---: | --- | --- | --- | --- |
| 운영자 발송 UI | 1 | 아니오 | 관리자 인증, 알림 템플릿, 대상 범위, 발송 취소, audit log, QA fixture | 중간 | 홈페이지/관리 페이지에서 알림 생성/검수 정책 먼저 설계 |
| push 알림 | 2 | 아니오 | 운영자 발송 정책, opt-out, permission UX, token 저장, delivery log, 개인정보 고지 | 높음 | FCM token/RLS/권한 UX 설계 후 앱 연결 |
| 홈 위젯 | 3 | 아니오 | Android AppWidget privacy, snapshot 데이터, 업데이트 주기, 배터리 정책 | 중간-높음 | native widget 트랙에서 재설계 |
| 무지개다리 | 4 | 아니오 | 감정 민감 문구, 노출 빈도, 문의/상품/결제 정책 | 높음 | UX copy와 one-time suggestion 정책 먼저 확정 |

## 13. 2026-07-09 프리미엄 보상 모달 closeout

활동·칭호 대시보드의 성장 피드백을 보완하기 위해 XP/포인트 획득 순간에만 `PremiumRewardModal`을 표시한다. 서버 XP ledger, level summary, title, RLS/RPC 계약은 변경하지 않는다.

### 정책

- XP 또는 산책 streak 보상이 있는 작성 flow에서만 표시한다.
- XP 지급이 없는 단순 완료 flow는 `PremiumNoticeModal`을 사용한다.
- `오늘 하루 안 보기`는 KST 기준 user-scoped local preference다.
- 같은 날 다시 표시하지 않고, 다음 날에는 다시 표시될 수 있다.
- preference는 presentation-only 상태이며 XP ledger, activity dashboard, notification state와 섞지 않는다.

### Android evidence

- 산책 XP 지급 직후 모달:
  - `/tmp/nuri-qa/v111-premium-reward-modal-walk-xp.png`
  - 표시값: `+30 XP`, `누적 XP 150`, `현재 레벨 Lv.2`, `1일 연속 산책`
- 오늘 하루 안 보기:
  - `/tmp/nuri-qa/v111-premium-reward-modal-hide-today.png`
- cold start 후 persistence:
  - `/tmp/nuri-qa/v111-premium-reward-modal-cold-start-persistence.png`
- 같은 날 후속 작성 suppress:
  - `/tmp/nuri-qa/v111-premium-reward-modal-suppressed-after-hide.png`

### 판정

- V1.1.1 1차 기능은 프리미엄 보상 모달 visual closeout 기준으로 release blocker 없음.
- Lv.1~100, 장기 summary RPC, privacy-limited `누리 랭킹`은 V1.1.1 고도화 범위에서 구현됐다. 공개 경쟁형 리더보드, streak bonus 자동 지급, push 실제 발송은 계속 Phase 2 또는 Parking이다.
- 2026-07-11 기준 PremiumRewardModal은 Lv.100/max 상태에서 `최고 레벨 달성`과 100% progress bar를 직접 표시한다. fixture auth 세션이 없는 경우에도 component presentation과 focused test로 max 상태 표시 계약을 고정한다.
