# 7차 V1.1 개발 작업서

업데이트: 2026-06-05

## 범위

이번 V1.1 1순위 마일스톤은 산책/location discovery를 자체 POI DB + Supabase PostGIS 기반 read path로 전환하는 작업이다.

V1.0 기능은 재오픈하지 않는다. Play Store 제출 자산은 전체 개발/QA 종료 후 최종 제출 직전 준비로 유지한다.

## 현재 완료

- `20260604110000_walk_poi_postgis_foundation.sql` remote apply 완료
- `walk_pois` canonical table 생성
- source/import/review/audit/alias layer 생성
- public RPC 생성
  - `walk_poi_public_nearby_v1`
  - `walk_poi_public_search_v1`
  - `walk_poi_public_detail_v1`
- admin dry-run RPC 생성
  - `walk_poi_admin_import_dry_run_v1`
- 앱 산책/location discovery read path에 POI RPC 우선 + Kakao fallback 병행 연결
- Android 현재 위치 smoke용 approved operator seed 3건 반영
- admin import commit/review workflow remote apply 완료
- public projection safety 검증 완료
- seed coverage/fallback 축소 gate 문서화 완료

## Read Path 정책

1. 앱은 산책 도메인에서 자체 POI RPC를 먼저 호출한다.
2. 자체 POI 결과가 있으면 NURI canonical POI 결과를 우선 표시한다.
3. 자체 POI 결과가 없거나 RPC가 실패하면 기존 `location-discovery-seed` Edge Function 경로로 fallback한다.
4. Kakao Local은 이번 단계에서 삭제하지 않는다.
5. canonical POI는 provider thumbnail enrichment를 요청하지 않는다.

## 최소 Seed 정책

- seed는 대량 import가 아니라 Android smoke용 최소 approved row다.
- source provider는 `operator-seed`로 기록한다.
- import batch/source record/review/audit 이력을 남긴다.
- rollback은 `external_source_id` prefix `nuri-v1.1-smoke-` 기준으로 수행할 수 있다.
- 1차 서울시청 fallback seed는 실기기 현재 위치 radius 밖이라 public projection에서 제외하고, 최종 public seed는 일산3동/주엽동 현재 위치 주변 3건으로 고정한다.

Seed 파일:

- `docs/sql/산책-위치기반-기능/v1.1-walk-poi-smoke-seed-2026-06-04.sql`
- `docs/sql/산책-위치기반-기능/v1.1-walk-poi-smoke-seed-current-location-2026-06-04.sql`

## Admin Import Dry-run Contract

필수 payload 필드:

- `externalSourceId`
- `name`
- `category`
- `categoryLabel`
- `address`
- `roadAddress`
- `latitude`
- `longitude`
- `aliases`
- `attribution`
- `confidenceScore`
- `qualityScore`
- `rawPayload`

Conflict key 후보:

- `source_provider + externalSourceId`
- normalized name + address
- coordinate proximity
- alias overlap

후속 commit/review 정책:

- dry-run은 실제 public 노출을 만들지 않는다.
- commit은 source record와 review 후보를 만든다.
- public read는 approved/public/active canonical row만 RPC로 반환한다.

## Admin Import Commit / Review Contract

RPC:

- `walk_poi_admin_import_commit_v1`
- `walk_poi_admin_review_v1`

정책:

1. `walk_poi_admin_import_commit_v1`은 admin/super_admin만 호출할 수 있다.
2. commit은 import batch, hidden/pending canonical candidate, source record, pending review row, audit log를 만든다.
3. duplicate는 `source_provider + externalSourceId` 기준으로 skip/count 처리한다.
4. `walk_poi_admin_review_v1`은 `approve`, `reject`, `held` action을 지원한다.
5. approve된 row만 `approved/public/active`로 public RPC에 노출한다.
6. rejected/held/pending row는 public RPC에서 반환하지 않는다.
7. 모든 action은 review/audit log에 남긴다.

상세 문서:

- `docs/domains/산책-위치기반-기능/v1.1-walk-poi-admin-import-review.md`
- `docs/domains/산책-위치기반-기능/v1.1-walk-poi-seed-strategy.md`

## Coverage / Fallback Gate

이번 단계에서는 Kakao Local fallback을 줄이지 않는다. 축소 gate만 정의한다.

Gate 기준:

- 우선 지역 approved/public/active POI count 충분
- nearby/search hit-rate 안정
- empty state 비율 안정
- public RPC error rate 안정
- rejected/held/pending public 누수 0건
- admin import rollback/held 기준 준비
- Android 실기기 리스트/검색/상세 smoke 통과

fallback 축소 전까지 앱은 POI RPC 우선 + 기존 `location-discovery-seed` fallback 병행 구조를 유지한다.

## 2026-06-05 Seed Coverage Expansion

- 대상 지역: 일산/주엽/호수공원 생활권, Android smoke 좌표 `37.676492,126.767888` 주변
- 방식: `walk_poi_admin_import_commit_v1`로 candidate/source/review/audit을 만들고 `walk_poi_admin_review_v1` approve로 public projection에 승격
- 신규 approved seed: 17건
- 총 public nearby 결과: 반경 5km 20건
- 검색 smoke alias: `ilsan0605`
- rollback: `docs/sql/산책-위치기반-기능/v1.1-walk-poi-ilsan-juyeop-seed-rollback-2026-06-05.sql`
- Kakao Local fallback: 유지. 이번 단계에서는 삭제하지 않음
- Android smoke: same-signature release update install 후 산책 리스트, `ilsan0605` 검색, `장촌공원` 상세 진입 확인. `zzzwalkpoi` empty/fallback UX는 update 전 동일 DB/read path에서 확인했고, update 후 마지막 logcat 재조회는 ADB 연결 끊김으로 환경 문제 분류

Fallback reason marker:

- `poi_disabled`
- `coordinate_missing`
- `poi_empty`
- `poi_rpc_error`
- `detail_not_found`

축소 gate 수치:

- 반경 3km approved/public/active POI 10건 이상
- 반경 5km approved/public/active POI 20건 이상
- 주요 검색어 hit-rate 70% 이상
- nearby empty result 비율 20% 이하
- fallback 호출 비율 30% 이하
- POI RPC error rate 1% 이하
- public projection 누수 0건

## 다음 구현 단위

1. 고양시 전체 seed coverage batch를 공공데이터/OSM/운영자 승인 seed로 확장한다.
2. admin read-only UI 운영 결과를 기준으로 write action UI 후보를 설계한다.
3. coverage 충족 지역부터 Kakao Local fallback 축소를 추가 적용한다.
4. 앱 read path는 POI RPC 우선 구조를 유지하되, Kakao Local 사용자 runtime 제거는 별도 gate로 분리한다.

## 2026-06-05 Admin Read-only UI / Fallback Gate 적용

- `walk_poi_admin_read_summary_v1` RPC를 추가해 import batch, review queue, audit log, coverage/fallback gate summary를 read-only로 제공한다.
- 앱에는 `산책 POI 운영` admin/super_admin 전용 화면을 추가했다.
- 일반 사용자에게 운영 메뉴는 노출하지 않고, 직접 route 접근 시에도 화면 내부 role gate로 차단한다.
- write action UI는 만들지 않았다. approve/reject/held 처리는 기존 admin RPC smoke 계약으로만 유지한다.
- 일산/주엽/호수공원 coverage region 안에서 POI RPC 정상 0건이면 Kakao fallback을 제한한다.
- region 밖, 좌표 없음, POI RPC error, feature flag off, detail missing은 기존 fallback을 유지한다.
- 전국 확장 로드맵은 `docs/domains/산책-위치기반-기능/v1.1-walk-poi-national-expansion.md`를 source로 둔다.

## Android 실기기 smoke 결과

- 기기: `R5CY613NMSY` / `SM_S937N`
- 산책 리스트: current-location seed 3건 표시 확인
- 산책 상세: `문화공원 오거리공원` 상세 진입 확인
- 산책 검색: `smoke` alias 검색으로 POI search 결과 표시 확인
- empty/fallback: `zzzwalkpoi` 검색에서 POI 0건 fallback과 empty UX 확인
- logcat: fatal / ANR / unhandled promise / ReactNativeJS fatal pattern 0건

## 이번 단계에서 하지 않는 것

- Kakao Local runtime hard delete
- 대량 OSM/public-data import
- MapLibre/PMTiles 도입
- 자체 타일 서버 구현
- V1.0 기능 재검증
