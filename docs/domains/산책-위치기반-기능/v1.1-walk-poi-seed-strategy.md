# V1.1 Walk POI Seed Strategy

업데이트: 2026-06-06

## 목적

V1.1 자체 POI DB 전환에서 seed coverage를 넓히기 전 운영 데이터 오염을 막고, seed provenance와 rollback 기준을 명확히 하기 위한 문서다.

## 현재 Seed 상태

Android 실기기 smoke를 위해 current-location 주변 approved operator seed 3건을 보존한다.

- 문화공원 오거리공원
- 강선공원
- 일산호수공원

Seed source:

- `operator-seed`
- import batch/source record/review/audit 이력 포함
- 대량 import 아님
- V1.1 smoke starter seed

Seed SQL:

- `docs/sql/산책-위치기반-기능/v1.1-walk-poi-smoke-seed-2026-06-04.sql`
- `docs/sql/산책-위치기반-기능/v1.1-walk-poi-smoke-seed-current-location-2026-06-04.sql`

## 2026-06-05 일산/주엽 생활권 Coverage 확대

V1.1 네 번째 단계에서는 실기기 smoke 좌표 `37.676492,126.767888` 주변 1개 생활권으로 seed coverage를 확장했다. 전국/수도권 대량 import가 아니라 일산/주엽/호수공원 반경 5km 내 산책 UX에 자연스러운 공원, 산책로, 수변 지점 중심의 운영자 승인 seed다.

반영 방식:

- admin import commit RPC: `walk_poi_admin_import_commit_v1`
- admin review RPC: `walk_poi_admin_review_v1`
- source provider: `operator-seed`
- import batch: `92391525-2f2a-4229-9288-26c647ba3eb9`
- 신규 approved/public/active seed: 17건
- 기존 starter seed 포함 반경 5km public nearby 결과: 20건
- 검색 smoke alias: `ilsan0605`
- audit log: commit/review action 기준 34건 확인

추가 seed:

- 주엽공원
- 문촌공원
- 후곡공원
- 한수공원
- 노래하는분수대 산책광장
- 일산문화공원
- 정발산공원
- 마두공원
- 장촌공원
- 대화공원
- 탄현근린공원
- 대화레포츠공원
- 일산호수공원 장미원
- 일산호수공원 메타세쿼이아길
- 고양종합운동장 산책로
- 킨텍스온누리공원
- 원마운트 수변산책로

Seed/rollback SQL:

- seed: `docs/sql/산책-위치기반-기능/v1.1-walk-poi-ilsan-juyeop-seed-coverage-2026-06-05.sql`
- rollback: `docs/sql/산책-위치기반-기능/v1.1-walk-poi-ilsan-juyeop-seed-rollback-2026-06-05.sql`

Rollback은 delete가 아니라 admin review RPC의 `held` action으로 public projection에서 제외한다. source/review/audit provenance는 보존한다.

## 2026-06-06 고양시 1차 Coverage 확대

V1.1 여섯 번째 단계에서는 고양시 전체 확장의 1차 batch를 일산동구, 일산서구, 덕양구 주요 공원/산책 지점 중심으로 반영했다. 전국 대량 import가 아니라 고양시 생활권 확장 batch이며, 사용자 runtime 외부 지도 API 호출 없이 운영자 seed 보조로만 OSM/Nominatim 후보 확인을 사용했다.

반영 방식:

- admin import commit RPC: `walk_poi_admin_import_commit_v1`
- admin review RPC: `walk_poi_admin_review_v1`
- source provider: `osm`
- source/attribution: `OpenStreetMap contributors (ODbL) · NURI operator review · Goyang coverage · 2026-06-06`
- import batch: `1d6bf293-c51d-4c6b-bdb8-74eec00989d6`
- 신규 approved/public/active seed: 33건
- 총 approved/public/active POI: 53건
- 검색 smoke alias: `goyang0606`
- commit 결과: created 33, duplicate 0, conflict 0, skipped 0, review 33
- review 결과: approve 33

대표 coverage 측정:

| 생활권 | 3km approved | 5km approved | gate 판정 |
| --- | ---: | ---: | --- |
| 일산/주엽/호수공원 | 31 | 37 | 유지 |
| 백석/마두/정발산 | 15 | 32 | 확장 적용 |
| 화정 | 8 | 15 | fallback 유지 |
| 행신 | 7 | 11 | fallback 유지 |
| 삼송 | 2 | 8 | fallback 유지 |
| 원당 | 6 | 18 | fallback 유지 |

검색 smoke:

- `goyang0606`: 33건
- `화정`: 4건
- `행신`: 2건
- `삼송`: 0건
- `백석`: 1건

Seed/rollback SQL:

- seed: `docs/sql/산책-위치기반-기능/v1.1-walk-poi-goyang-seed-coverage-2026-06-06.sql`
- rollback: `docs/sql/산책-위치기반-기능/v1.1-walk-poi-goyang-seed-rollback-2026-06-06.sql`

Rollback은 delete가 아니라 admin review RPC의 `held` action으로 public projection에서 제외한다. OSM/source/review/audit provenance는 보존한다. 화정/행신/삼송/원당은 아직 검색 hit와 3km/5km coverage 기준이 부족하므로 Kakao fallback을 유지한다.

Android smoke:

- release APK를 같은 서명으로 `adb install -r` 업데이트해 이번 JS bundle을 반영했다.
- 산책 리스트 진입과 starter seed 표시를 확인했다.
- `ilsan0605` 검색에서 신규 seed `장촌공원`, `한수공원` 표시를 확인했다.
- `장촌공원` 상세 진입과 주변 산책 장소 표시를 확인했다.
- `zzzwalkpoi` empty/fallback UX는 update 전 동일 DB/read path에서 crash 없이 확인했다.
- update 후 마지막 fallback/logcat 재확인 중 ADB 기기 연결이 끊겨 logcat 회수는 환경 문제로 분류한다.

## Admin Workflow Smoke Data

admin import commit/review workflow 검증용 row는 `nuri-v1.1-admin-smoke-` prefix를 사용한다.

운영 기준:

- 검증 중 approved/public 상태로 잠깐 승격할 수 있다.
- 검증 종료 시 `held/hidden`으로 내려 public projection에서 제외한다.
- delete가 아니라 held 처리로 audit trail을 유지한다.
- public search/nearby/detail RPC에서 admin smoke row가 노출되면 blocker다.

Cleanup SQL:

- `docs/sql/산책-위치기반-기능/v1.1-walk-poi-admin-workflow-smoke-cleanup-2026-06-05.sql`

## Seed Coverage 확대 기준

seed coverage 확대는 아래 기준을 만족할 때만 진행한다.

- source provenance가 명확함
- 좌표가 WGS84 기준으로 검증 가능함
- category가 `walk_pois.category` enum에 매핑 가능함
- duplicate/conflict 검토 기준이 있음
- admin review queue로 pending 후보를 분리할 수 있음
- rejected/held/pending이 public projection에 노출되지 않음

초기 대상:

- 공공데이터 park/green space source
- OSM park/trail/path 후보
- 운영자 승인 seed
- Kakao Local은 사용자 runtime이 아니라 admin seed 보조 후보로만 제한

## Fallback 축소 전 기준

Kakao fallback 축소는 seed 양만으로 판단하지 않는다.

필수 gate:

- 반경 3km 내 approved/public/active POI 10건 이상
- 반경 5km 내 approved/public/active POI 20건 이상
- 주요 검색어 hit-rate 70% 이상
- nearby empty result 비율 20% 이하
- fallback 호출 비율 30% 이하
- public RPC error rate 1% 이하
- public projection 누수 0건
- rollback/held 처리 절차 준비
- Android 실기기 산책 리스트/검색/상세 smoke 통과

축소하지 않는 조건:

- coverage가 낮아 사용자가 기존보다 결과를 적게 받음
- fallback 호출률이 높음
- 운영자가 source provenance를 설명할 수 없음
- seed conflict가 unresolved 상태임

## Rollback / Held 정책

운영 데이터 삭제는 기본 전략이 아니다. 문제가 생긴 seed는 우선 public projection에서 제외한다.

우선 순서:

1. `walk_pois.review_status = 'held'`
2. `walk_pois.visibility_status = 'hidden'`
3. `walk_poi_source_records.candidate_status = 'held'`
4. audit log에 조치 이유 기록

delete는 잘못 적재된 raw payload나 개인정보성 데이터가 들어간 경우에만 별도 승인 후 수행한다.

## 2026-06-05 Fallback Gate 제한 적용

다섯 번째 단계에서는 일산/주엽/호수공원 생활권에 한해 Kakao Local fallback 축소 gate를 코드에 적용했다.

적용 기준:

- center: `37.676492,126.767888`
- radius: 5km
- gate flag: `EXPO_PUBLIC_ENABLE_WALK_POI_FALLBACK_GATE=true`
- 제한 조건: coverage region 안 + POI RPC 정상 응답 + POI 0건
- 유지 조건: `poi_disabled`, `coordinate_missing`, `poi_rpc_error`, `detail_not_found`, coverage region 밖

결과:

- coverage region 안에서 POI 0건이면 기존 Kakao fallback으로 자동 전환하지 않고 자체 POI empty UX로 닫는다.
- region 밖과 안전 fallback 조건에서는 기존 `location-discovery-seed` + Kakao Local 서버 경유 fallback을 유지한다.
- Kakao Local 사용자 runtime은 이번 단계에서 삭제하지 않았다.

## 2026-06-06 Fallback Gate 확장 판정

고양시 전체를 하나의 gate로 묶지 않는다. 2026-06-06 coverage 측정 기준으로 gate는 아래 생활권에만 적용한다.

- `ilsan_juyeop_lakepark`: 일산/주엽/호수공원 생활권, center `37.676492,126.767888`, radius 5km
- `baekseok_madu_jeongbalsan`: 백석/마두/정발산 생활권, center `37.6433,126.7882`, radius 5km

fallback 제한 조건은 변하지 않는다.

- 제한: coverage region 안 + POI RPC 정상 응답 + POI 0건
- 유지: `poi_disabled`, `coordinate_missing`, `poi_rpc_error`, `detail_not_found`, coverage region 밖

화정/행신/삼송/원당은 다음 coverage batch에서 alias와 POI 수를 보강한 뒤 gate를 재판정한다.

## 다음 작업

다음 단계는 서울 주요 산책 권역 seed coverage 확장 또는 admin write UI 고도화다. fallback 축소는 일산/주엽/호수공원, 백석/마두/정발산처럼 coverage 기준을 충족한 region에만 제한 적용하며, 전국 fallback 제거는 별도 마일스톤으로 분리한다.
