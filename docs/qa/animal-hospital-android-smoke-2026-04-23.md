# AnimalHospital Android Smoke 2026-04-23

- device: `R5CY613NMSY / SM_S937N`
- app build: local Android debug build after `동물병원 위치 정확도 상태머신 보강`, `동물병원 provider enrichment 배치 확장`, `동물병원 민감 검수 import` 반영
- executed_at: `2026-04-23 19:33 KST`

## Scope

1. 전체메뉴 > 우리동네 동물병원 진입
2. 기본 가까운순 리스트 상태
3. `24시 운영` 필터와 승인 샘플 노출
4. `특수동물병원` 필터 empty state
5. 상세 진입
6. `전화하기` `tel:` intent
7. `길찾기` 외부 지도 chooser
8. 지도 preview `열기` chooser

## Result

- 기본 가까운순 진입에서 `가까운순 / 24시 운영 / 특수동물병원` 칩만 노출되고 `전체`, `최근검색어`는 노출되지 않았다.
- 기본 가까운순 리스트에서 `현재 위치 기준 / 일산3동`이 보였고, 리스트 카드 주소는 노출되지 않았다.
- 첫 카드 `24시 마이동물의료센터`에서 official thumbnail이 보였고 전화번호가 `031-945-5000` 하이픈 포맷으로 노출됐다.
- 같은 화면에서 검수되지 않은 병원은 `전화번호 확인 중` fallback으로 유지됐다.
- `24` 검색 후 `24시 운영` 칩을 선택하면 `24시동탄시티동물의료센터`만 남았고 전화번호 `031-8003-7533`이 하이픈 포맷으로 노출됐다.
- `24` 검색 후 `특수동물병원` 칩을 선택하면 `검수된 특수동물 진료 병원이 아직 없어요` empty state가 노출됐다.
- `24시 마이동물의료센터` 상세에서 hero thumbnail, 주소, 거리, `031-945-5000`, `전화하기`, `길찾기`, 지도 preview, `열기` CTA가 모두 노출됐다.
- `전화하기` 탭 시 삼성 다이얼러에 `031-945-5000` 번호가 채워진 상태로 전환됐다.
- `길찾기` 탭 시 `연결 프로그램` chooser가 열렸고 `네이버지도 / 지도 / 카카오맵 / TMAP`이 노출됐다.
- 지도 preview 섹션의 `열기` 탭도 동일한 `연결 프로그램` chooser로 정상 연결됐다.
- 상세 진입 후 뒤로가기로 앱 복귀가 정상 동작했고 치명 로그, red screen, 무한 로딩 재발은 확인되지 않았다.

## Notes

- 현재 실기기에서 `24시 마이동물의료센터`는 approved phone / coordinates / thumbnail sample로 다시 확인했다.
- `24시 운영`은 병원명 문자열이 아니라 approved `open24Hours=true` 샘플만 노출되는지 확인하기 위해 `24` 검색 + chip 조합으로 검증했다.
- 이번 smoke는 실기기 수동 검증이다. 스크린샷과 `uiautomator dump`는 `/tmp/nuri-qa` 작업 디렉터리에서 수집했다.
