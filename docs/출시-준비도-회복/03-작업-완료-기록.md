# 출시 준비도 회복 작업 완료 기록

상태: 진행 중
최종 갱신: 2026-04-03

## 배경

- 회복 작업은 완료 증적이 없으면 같은 일을 반복하게 된다.

## 현재 문제

- project-memory만으로는 액션별 세부 완료 이력이 빠르게 추적되지 않는다.

## 목표

- 하위 작업이 닫힐 때마다 같은 형식으로 완료 기록을 남긴다.

## 작업 목록

- 완료 기록 템플릿 유지
- 이번 턴에 닫힌 하위 작업 기록

## 완료 기준

- 새 하위 작업 완료 시 아래 템플릿으로 즉시 추가할 수 있다.

## 리스크

- 완료 기록 없이 상태만 바꾸면 다음 세션에서 근거가 사라진다.

## 다음 액션

- 액션 1~5 하위 완료 단위를 이 문서에 계속 누적한다.

## 상태

- 진행 중

## 기록 템플릿

## 2026-04-03 완료 기록

### 완료 ID

- `REL-2026-04-03-POLICY-LINK-DEVICE-QA-PO-SIGNOFF`

### 작업명

- 정책 문서 앱 링크 실기기 검증 종료와 PO 최종 승인 반영

### 완료일

- 2026-04-03

### 무엇을 했는지

- `이용약관`, `개인정보처리방침`, `계정 삭제 / 탈퇴 안내` 3종에 대해 이미 연결된 Notion public URL 경로를 Android 실기기 `SM_S937N`에서 PO가 직접 다시 확인했다.
- 회원가입 화면의 `이용약관 동의`, `개인정보처리방침 동의`, More Drawer의 `계정 삭제 > 삭제 안내 상태 확인`이 모두 외부 브라우저로 열리고 다시 앱으로 정상 복귀함을 PO 확인 사실로 잠갔다.
- 정책 문서 작업 상태를 `최종 완료 3종`과 `보류 2종`으로 분리했다.
  - 최종 완료: `이용약관`, `개인정보처리방침`, `계정 삭제 / 탈퇴 안내`
  - 보류: `마케팅 수신 안내(draft 유지)`, `커뮤니티 운영정책(public URL만 존재, 앱 연결 대상 아님)`

### 검증 결과

- physical-device:
  - `SM_S937N`에서 3개 경로의 외부 브라우저 팝업과 앱 복귀가 정상 확인됐다.
- 승인:
  - PO가 동일 경로를 직접 확인하고 Task 4 공식 승인 완료를 통보했다.
- 상태:
  - `작성 완료 + Notion 반영 완료 + 앱 링크 연결 완료 + 실기기 검증 완료 + PO 최종 승인 완료`
  - 위 상태는 `이용약관`, `개인정보처리방침`, `계정 삭제 / 탈퇴 안내` 3종에만 적용한다.

### 남은 리스크

- `마케팅 수신 안내`는 여전히 `draft`다.
- `커뮤니티 운영정책`은 public URL은 있으나 앱 연결 대상이 아니다.
- 운영자 정보, 문의처, 법무 검토는 별도 확인 항목으로 남는다.

### 다음 연결 작업

- 정책 문서 release blocker가 해제됐으므로, 다음 우선순위는 장소 / 여행 / 산책 physical-device QA 최종 증적이다.

## 2026-04-02 완료 기록

### 완료 ID

- `REL-2026-04-02-STEP3-POST-AUTO-HIDE-ROW-EVIDENCE`

### 작업명

- Step 3 community moderation post auto-hide row-level 증적 확보와 restore 경로 fact check

### 완료일

- 2026-04-02

### 무엇을 했는지

- linked remote baseline을 다시 조회해 `reports = 27`, `community_moderation_queue = 20`, `community_moderation_actions = 3`, `community_image_assets = 17`로 고정했다.
- 기존 1차 target `657a66aa-347c-4e6a-8b0d-ebf4a2c98bd2`는 여전히 `active`, `unique_reporter_count = 1` baseline이라는 점을 다시 잠갔다.
- 새 QA target `317f7b2e-5458-4352-841f-94a8db7bec73`에서 `report_count = 3`, `unique_reporter_count = 3`, `auto_hide_threshold`, `active -> auto_hidden` action row까지 실제로 남았음을 row ID 기준으로 확보했다.
- 같은 target에는 이미지 row가 없어 `cleanup_pending`은 이번 턴에 못 닫는 대신, linked remote 전체의 historical image cleanup trace 1건을 다시 확인했다.
- repo SQL에 있는 restore/resolved 경로도 linked remote에 직접 호출해 봤고, `reports_status_check` mismatch 때문에 기본 restore 경로가 아직 깨져 있다는 사실을 확인했다.

### 검증 결과

- linked remote:
  - `public.posts.id = 317f7b2e-5458-4352-841f-94a8db7bec73`
  - `status = auto_hidden`
  - `community_moderation_queue.id = 481f2b93-0b30-413c-be0c-c08fc39a7dbe`
  - `report_count = 3`
  - `unique_reporter_count = 3`
  - `community_moderation_actions.id = b19f8f17-90d8-432e-95fb-8c2dd13fb03f`
  - `before_status = active`
  - `after_status = auto_hidden`
- restore path fact check:
  - `apply_community_moderation_action(... p_after_status = 'active')`
  - 결과: `reports_status_check` 제약 위반으로 실패
  - 영향: auto-hide row는 그대로 보존

### 남은 리스크

- 이번 턴은 `post auto-hide` 증적을 닫았지만, 같은 target의 `cleanup_pending`은 이미지 부재로 미확보다.
- `resolved / restore` 기본 경로는 repo SQL과 remote constraint가 어긋나 현재 그대로는 운영 완료라고 쓸 수 없다.
- 실기기 자동 입력은 adb 문자열 주입 한계가 있어 read-path 캡처를 완전히 자동화하지 못했다.

### 다음 연결 작업

- 정책 문서 외부 공개와 앱 링크 실제 연결 상태를 사용자 우선순위 기준 다음 작업으로 이어간다.

## 2026-03-31 완료 기록

### 완료 ID

- `REL-2026-03-31-COMMUNITY-KEYBOARD-SENSORY-QUALITY-REFINE`

### 작업명

- Step 3 신고 모달과 주요 입력 화면의 Android keyboard smoothness / premium branding 보정

### 완료일

- 2026-03-31

### 무엇을 했는지

- Fabric/New Architecture 환경에서 의미 없는 `LayoutAnimation` 경로를 제거했다.
- `useKeyboardInset()`의 `setLayoutAnimationEnabledExperimental`과 `LayoutAnimation` 로직을 완전히 삭제했다.
- `CommunityDetailScreen` 신고 모달은 `react-native-keyboard-controller`의 `KeyboardAvoidingView`로 원래 중앙 위치를 유지한 채 keyboard padding만 반영하도록 다시 구성했다.
- `NicknameSetupScreen`, `WeatherActivityRecordScreen`, `RecordTagModal`도 같은 keyboard-controller `KeyboardAvoidingView` 축으로 맞추고, safe-area와 최외곽 배경색을 화면 배경과 일치시켰다.

### 검증 결과

- local:
  - `yarn tsc --noEmit` 통과
  - `yarn test --watchAll=false --watchman=false __tests__/authNotices.test.ts __tests__/appBoot.test.ts __tests__/authStoreRecovery.test.ts __tests__/passwordRecoverySession.test.ts` 통과
- adb:
  - `adb logcat -c` 후 앱 재실행
  - `FATAL EXCEPTION`, `AndroidRuntime`, `IllegalViewOperationException`, `ReactNativeJS` 신규 로그 없음

### 남은 리스크

- repo 기준 부드러움 보정은 끝났지만, physical-device에서 실제 키보드 show/hide 감각과 신고 모달 색감은 다시 확인해야 한다.
- Step 3 auto-hide 증적은 여전히 미완료다.

### 다음 연결 작업

- 실기기에서 신고 모달 keyboard transition과 premium modal 인상을 재확인한 뒤, 같은 target post로 auto-hide threshold 검증을 이어간다.

### 완료 ID

- `REL-2026-03-31-COMMUNITY-REPORT-ROW-EVIDENCE-AND-KEYBOARD-ACCESSIBILITY`

### 작업명

- Step 3 community 신고 row-level 1차 증적 확보와 keyboard accessibility blocker fix

### 완료일

- 2026-03-31

### 무엇을 했는지

- linked remote baseline `reports = 23`, `community_moderation_queue = 18`, `community_moderation_actions = 2` 이후 실기기 신고 1건이 실제로 반영됐는지 다시 조회했다.
- 새 신고 row `0742e21d-c359-4075-b55f-3da49a0e06ae`와 moderation queue row `c7db6eed-fe3a-465b-9057-b175ef1370a2`를 확인했고, target post `657a66aa-347c-4e6a-8b0d-ebf4a2c98bd2`는 아직 `active` 상태임을 잠갔다.
- 동시에 Android 실기기에서 키보드가 `신고하기` 버튼을 가리는 blocker를 확인해 `CommunityDetailScreen` 신고 모달을 `KeyboardAvoidingView + ScrollView + safe-area padding + blank-space dismiss` 구조로 보정하고, 신고 완료 피드백을 premium modal로 전환했다.
- 전역 입력 화면 audit 결과 keyboard-aware 보호가 이미 있는 화면과 없는 화면을 분리했고, 누락된 `NicknameSetupScreen`, `WeatherActivityRecordScreen`, `RecordTagModal`에 같은 방어선을 추가했다.

### 검증 결과

- linked remote:
  - `reports = 24`
  - `community_moderation_queue = 19`
  - `community_moderation_actions = 2`
  - 새 queue row `unique_reporter_count = 1`, `queue_status = open`
  - target post `status = active`
- local:
  - `yarn tsc --noEmit` 통과
  - `yarn test --watchAll=false --watchman=false __tests__/authNotices.test.ts __tests__/appBoot.test.ts __tests__/authStoreRecovery.test.ts __tests__/passwordRecoverySession.test.ts` 통과

### 남은 리스크

- Step 3 auto-hide 자체는 아직 닫히지 않았다. 현재 신고는 1건뿐이라 `community_moderation_actions`가 증가하지 않았고, target post도 `active` 상태를 유지한다.
- Guide/PetTravel/More의 search-only 입력은 하단 CTA blocker가 없어서 이번 턴에 건드리지 않았다.

### 다음 연결 작업

- 같은 target post에 대해 다른 reporter를 추가해 auto-hide threshold를 넘긴 뒤 `community_moderation_actions`와 content status 전환을 검증한다.

### 완료 ID

- `REL-2026-03-31-ACCOUNT-DELETION-STORAGE-CLEANUP-CLOSEOUT`

### 작업명

- 계정 삭제 cleanup queue 강제 실행과 bucket orphan 0건 검증

### 완료일

- 2026-03-31

### 무엇을 했는지

- request `be833a80-31b1-44b3-8d95-cf9ec051fdd1` 기준 `account_deletion_cleanup_items` 17건과 target bucket path 17건을 다시 조회해 cleanup baseline을 잠갔다.
- remote `claim_account_deletion_cleanup_items(20)` RPC를 직접 실행해 봤고, `request_id is ambiguous` 오류로 실제 claim worker가 현재 깨져 있다는 사실을 확인했다.
- 이번 턴에서는 request를 `cleanup_pending`으로 올리고 cleanup item 17건을 직접 `processing`으로 전환한 뒤, `supabase storage rm --yes --experimental --linked`로 `community-images 15건`, `pet-profiles 2건`을 실제 삭제하고 `complete_account_deletion_cleanup_item()`으로 전부 `completed` 처리했다.

### 검증 결과

- linked remote:
  - `account_deletion_cleanup_items.status = completed` 17건
  - `account_deletion_requests.status = completed`
  - `storage_cleanup_pending = false`
  - `cleanup_completed_count = 17`
  - `cleanup_completed_at = 2026-03-31 06:22:04.101837+00`
- storage orphan 검증:
  - `storage.objects` join 기준 `community-images = 0`, `pet-profiles = 0`
  - `supabase storage ls --experimental --linked -r ss:///pet-profiles/74cd98ab-5c3c-4502-b6cc-6543d8bd6a70`: 빈 결과
  - `supabase storage ls --experimental --linked -r ss:///community-images/74cd98ab-5c3c-4502-b6cc-6543d8bd6a70`: 빈 결과

### 남은 리스크

- cleanup worker의 공식 claim RPC인 `claim_account_deletion_cleanup_items()`는 remote에서 `request_id` ambiguous 오류로 깨져 있다.
- 이번 완료는 release blocker 증적을 닫는 데는 충분하지만, 운영 자동화 경로는 별도 engineering follow-up이 필요하다.

### 다음 연결 작업

- Step 3 community moderation 실기기 QA를 수행해 `reports`, `community_moderation_queue`, `community_moderation_actions` row-level 증적을 확보한다.

### 완료 ID

- `REL-2026-03-31-GUEST-UX-PREMIUM-ROUTING-REFRESH`

### 작업명

- 게스트 UX premium modal 전환과 guest route/empty state 전면 정리

### 완료일

- 2026-03-31

### 무엇을 했는지

- 로그아웃, 계정 삭제, 비밀번호 재설정 성공 notice를 `SignIn` 라우트 기반 premium modal로 통일하고, 로그아웃/계정 삭제 완료 후 guest home이 아니라 `SignIn`으로 reset 이동하게 정리했다.
- 게스트 상태에서 타임라인은 공통 `GuestLockedState`로 잠그고, 방명록 placeholder는 제거한 뒤 게스트/로그인 상태 모두 브랜드 톤의 안내 화면으로 교체했다.
- 게스트 홈은 원격 dog placeholder를 제거하고 로컬 에셋 + 브랜드 컬러 기반 preview home으로 다시 구성했다.
- More drawer는 게스트 상태에서 로그아웃 버튼을 숨기고, 로그인 유도 항목만 노출하게 바꿨다.

### 검증 결과

- `yarn tsc --noEmit` 통과
- `yarn test --watchAll=false --watchman=false __tests__/authNotices.test.ts __tests__/appBoot.test.ts __tests__/authStoreRecovery.test.ts __tests__/passwordRecoverySession.test.ts` 통과

### 남은 리스크

- `가입되지 않은 이메일입니다` modal은 현재 Supabase의 generic invalid credential error를 기준으로 띄우므로, 기존 회원의 오입력 비밀번호와 완전하게 구분되지는 않는다.
- Step 2.5는 repo 기준 구현 완료이며, 실기기에서 guest home / guestbook / drawer / logout-delete notice의 시각 검증이 남아 있다.

### 다음 연결 작업

- 실기기에서 Step 2.5 화면과 라우팅을 먼저 재검증한 뒤, Step 3 community moderation QA로 넘어간다.

### 완료 ID

- `REL-2026-03-31-ACCOUNT-DELETION-HARD-DELETE-QA`

### 작업명

- 계정 삭제 실기기 Step 2 QA와 DB hard delete 증적 확보

### 완료일

- 2026-03-31

### 무엇을 했는지

- Android 실기기 `SM_S937N`에서 일반 사용자 QA 계정 `test1234@test.com`으로 계정 삭제를 실행했다.
- 삭제 전 baseline으로 `account_deletion_requests = 0`, `account_deletion_cleanup_items = 0`, `posts = 13`, `comments = 35`, `pets = 2`, `role = user`를 확인했다.
- 삭제 직후 linked remote를 조회해 `auth.users`, `profiles`, `posts`, `comments`, `pets`가 모두 0건으로 떨어졌고, `account_deletion_requests` 1건과 `account_deletion_cleanup_items` 17건이 생성된 것을 확인했다.

### 검증 결과

- 사용자 시각 검증:
  - 삭제 완료 토스트 노출
  - 동일 계정 재로그인 실패
  - 관리자 계정으로 커뮤니티 read-path 확인 시 기존 게시글 미노출
- linked remote:
  - `auth.users = 0`
  - `public.profiles = 0`
  - `public.posts = 0`
  - `public.comments = 0`
  - `public.pets = 0`
  - `public.account_deletion_requests = 1`
  - `public.account_deletion_cleanup_items = 17`
  - request status: `completed_with_cleanup_pending`

### 남은 리스크

- storage cleanup worker는 아직 실행되지 않아 cleanup item 17건이 모두 `pending`이다.
- `public.memories`, `public.memory_images`는 삭제 후 0건을 확인했지만, 이번 실행 전 baseline을 별도로 수집하지 않아 전후 비교형 강한 증적로는 약하다.

### 다음 연결 작업

- cleanup item `pending -> completed` 전환을 후속 확인하고, Step 3 community moderation 실기기 QA를 수행한다.

### 완료 ID

- `REL-2026-03-31-PASSWORD-RESET-PHYSICAL-QA-REVALIDATION`

### 작업명

- 비밀번호 재설정 recovery blocker 실기기 Step 1 재검증

### 완료일

- 2026-03-31

### 무엇을 했는지

- Android 실기기 `SM_S937N`에서 QA 계정 `shinjaejun1996@gmail.com`으로 비밀번호 재설정 메일 복귀 시나리오를 다시 수행했다.
- recovery 링크 클릭 후 `PasswordResetFormScreen` 정상 진입과 `test1234!` 변경 성공 뒤 `SignIn` 복귀를 실제 사용자 시각으로 확인했다.
- linked remote를 재조회해 계정 중복 생성이 없고, 이번 재검증에서 추가 pet row side effect가 없다는 사실을 다시 잠갔다.

### 검증 결과

- physical-device 시각 검증:
  - `PasswordResetFormScreen` 정상 진입
  - 비밀번호 변경 성공
  - 성공 문구와 함께 `SignIn` 복귀
- linked remote:
  - `auth.users`: 1건 유지
  - `public.profiles`: 1건 유지
  - `public.pets`: 2건 유지, 새 row 추가 없음

### 남은 리스크

- `2026-03-31 02:11:30 UTC` historical garbage pet row 1건은 여전히 운영/DB 정리 대상이다.
- Action 1 전체는 아직 진행 중이며, account deletion / community moderation row-level 증적이 남아 있다.

### 다음 연결 작업

- Step 2 account deletion 실기기 QA로 넘어가 row-level 증적을 확보한다.

### 완료 ID

- `REL-2026-03-31-PASSWORD-RESET-RECOVERY-SANDBOX-FIX`

### 작업명

- 비밀번호 재설정 recovery session sandbox 가드 및 cleanup guarantee 구현

### 완료일

- 2026-03-31

### 무엇을 했는지

- `authStore`에 persisted `passwordRecoveryFlow` 상태와 TTL을 추가해 recovery 세션을 앱 전역 로그인 상태와 분리했다.
- `AppProviders`에서 recovery session을 일반 로그인 bootstrap으로 승격하지 않게 막고, guest sandbox 상태에서 profile fetch / pet fetch / monitoring user 승격을 차단했다.
- `PasswordResetRecoveryScreen`, `PasswordResetFormScreen`에 recovery 진입/종료 경로를 정리해 성공, 에러, 뒤로가기, 취소 모두 `SignIn` reset으로 수렴시키고 임시 세션을 완전히 정리하게 바꿨다.
- `boot.ts`, `HomeScreen`, `MainScreen`에 2차 방어선을 추가해 recovery active 상태에서 `NicknameSetup`, `PetCreate(from: 'auto')`, 홈 자동 진입을 막았다.
- `SignInScreen`에는 stale recovery flag가 남아 정상 로그인까지 막지 않도록 local recovery cleanup 안전장치를 추가했다.

### 검증 결과

- `yarn test --watchAll=false --watchman=false __tests__/appBoot.test.ts __tests__/authStoreRecovery.test.ts __tests__/passwordRecoverySession.test.ts` 통과
- `yarn tsc --noEmit` 통과

### 남은 리스크

- repo 기준 fix만 완료됐고, physical-device Step 1 재검증 전까지 release blocker 해제는 아니다.
- 이번 버그로 생성된 `2026-03-31 02:11:30 UTC` pet row는 별도 운영/DB 정리 task로 남아 있다.

### 다음 연결 작업

- 실기기에서 `메일 링크 복귀 -> 비밀번호 변경 -> SignIn reset`을 다시 검증한다.

## 2026-03-30 완료 기록

### 완료 ID

- `REL-2026-03-30-EVIDENCE-SNAPSHOT-SCRIPT`

### 작업명

- release readiness 자동 증적 수집 스크립트 생성

### 완료일

- 2026-03-30

### 무엇을 했는지

- `scripts/collect_release_readiness_snapshot.sh`를 추가해 기본 QA, 타겟 테스트, linked Supabase migration/dry-run, Firebase 파일 배치, local-only runtime 파일 경계, tracked monitoring DSN 여부를 한 번에 확인할 수 있게 만들었다.

### 검증 결과

- 스크립트 실행 성공
- `yarn test:qa` 9/9 통과
- `petTravel / consents / legalDocuments / guideCatalogSource` 4/4 suite 통과
- linked remote migration 8개 정합 확인

### 남은 리스크

- physical-device, 메일 앱 복귀, account deletion row-level, community row-level 증적은 아직 외부 의존이다.

### 다음 연결 작업

- 자동 증적과 외부 증적을 액션 1 문서에 분리 반영한다.

### 완료 ID

- `REL-2026-03-30-MARKETING-DRAFT-SOURCE-LOCK`

### 작업명

- 마케팅 수신 안내 draft source 정렬

### 완료일

- 2026-03-30

### 무엇을 했는지

- `docs/policies/마케팅-수신-안내-초안.md`를 생성하고, `src/services/legal/documents.ts`에서 marketing 문서를 `draft + draftPath` 상태로 정렬했다.
- 기존 정책 초안 3개에서 `placeholder` 문의 문구를 제거했다.

### 검증 결과

- `__tests__/legalDocuments.test.ts` 추가 및 통과

### 남은 리스크

- 외부 공개 URL과 법무 확정 문안은 여전히 없다.

### 다음 연결 작업

- 정책 문서 외부 공개 URL 확정 후 앱 링크 실검증을 수행한다.

### 완료 ID

- `REL-2026-03-30-MONITORING-DSN-DEFAULT-REMOVAL`

### 작업명

- tracked monitoring DSN 기본값 제거

### 완료일

- 2026-03-30

### 무엇을 했는지

- `src/services/monitoring/config.ts`에서 tracked 기본 Sentry DSN을 제거해, 기본 tracked 코드에 실제 DSN이 남지 않도록 바꿨다.

### 검증 결과

- 자동 증적 스크립트에서 `monitoring config: tracked DSN not present` 확인

### 남은 리스크

- Supabase / Kakao / TourAPI는 아직 공식 env 주입 체계 없이 local-only 파일을 사용한다.

### 다음 연결 작업

- build variant별 secret 주입 방식 승인 후 env 체계를 도입한다.

### 완료 ID

- `REL-2026-03-30-GUIDE-REMOTE-ONLY-RUNTIME-LOCK`

### 작업명

- Guide 운영 빌드 source of truth 정리

### 완료일

- 2026-03-30

### 무엇을 했는지

- `src/services/guides/service.ts`에서 local seed fallback을 개발 빌드 한정으로 줄였고, 운영 빌드에서는 remote empty/error 시 test seed를 사용자 경로에 대신 보여주지 않도록 바꿨다.
- `src/services/guides/source.ts`에서 remote-empty + remote-error 설명 문구를 분리했다.

### 검증 결과

- `__tests__/guideCatalogSource.test.ts` 추가 및 통과

### 남은 리스크

- 운영 published row가 실제로 채워지지 않으면 Guide 화면은 빈 상태일 수 있다.

### 다음 연결 작업

- 운영 guide row 입력과 GuideAdmin 캡처를 evidence pack에 연결한다.

### 완료 ID

- `REL-2026-03-30-EVIDENCE-ROW-LEVEL-BASELINE`

### 작업명

- release blocker evidence pack row-level baseline 확보

### 완료일

- 2026-03-30

### 무엇을 했는지

- `adb devices -l`로 현재 연결 기기 inventory를 다시 확인해 물리 Android 미연결 사실을 증적으로 고정했다.
- linked remote에서 `account_deletion_requests`, `account_deletion_cleanup_items`, `reports`, `community_moderation_queue`, `community_moderation_actions`, `community_reporter_flags`, `community_image_assets` row-level snapshot을 다시 수집했다.
- `scripts/collect_release_readiness_snapshot.sh`에 디바이스 inventory, 계정 삭제 스냅샷, 커뮤니티 운영 스냅샷을 추가해 같은 근거를 다시 재현할 수 있게 만들었다.
- evidence pack 문서에 reset deep link local 전제조건, 계정 삭제 0건 상태, 커뮤니티 auto-hide/action/image row 존재를 분리 기록했다.

### 검증 결과

- `adb devices -l`: emulator 1대만 탐지, physical device 미탐지
- linked remote:
  - `account_deletion_requests = 0`
  - `account_deletion_cleanup_items = 0`
  - `reports = 23`
  - `community_moderation_queue = 18`
  - `community_moderation_actions = 2`
  - `community_reporter_flags = 1`
  - `community_image_assets = 17`
- 최신 moderation action 2건이 모두 `active -> auto_hidden` 전환으로 확인됨
- 최신 reporter flag 1건에서 `flag_status = watch`, `rate_limit_hit_count = 1` 확인

### 남은 리스크

- physical-device, reset mail return, account deletion execution proof, community cleanup_pending / resolved / restore proof는 여전히 미확보다.

### 다음 연결 작업

- 실기기 1대를 연결해 reset mail return과 계정 삭제 / 커뮤니티 시나리오를 실제 row-level 캡처로 이어간다.

### 완료 ID

- `REL-2026-03-31-TASK-2-9-UI-STABILIZATION`

### 작업명

- Step 3 전 선행 UI 안정화

### 완료일

- 2026-03-31

### 무엇을 했는지

- More 드로어의 `닉네임 수정`을 중앙 정렬 브랜드 모달로 전환하고 keyboard-controller 기반으로 정리했다.
- More 드로어의 `비밀번호 변경`을 내부 스크롤이 있는 반응형 모달로 재구성했다.
- `기록하기` 헤더 top safe area를 보정했다.
- logged-in home에 scroll persistence를 추가해 `오늘의 기록`, `일정 추가` 등에서 돌아와도 홈 스크롤 위치가 유지되게 했다.
- `일정 추가`, `펫 프로필 수정`, `펫 등록 1/2`, `펫 등록 2/2`를 scroll 중심 keyboard 대응으로 다시 정리했다.

### 검증 결과

- `yarn tsc --noEmit` 통과
- `yarn test --watchAll=false --watchman=false __tests__/authNotices.test.ts __tests__/appBoot.test.ts __tests__/authStoreRecovery.test.ts __tests__/passwordRecoverySession.test.ts` 통과
- `git diff --check` 통과

### 남은 리스크

- 이번 턴은 repo 기준 안정화다. Android 실기기에서 실제 keyboard 전환 감각과 홈 스크롤 복귀를 다시 봐야 한다.
- Step 3 auto-hide 증적은 아직 미완료다.

### 다음 연결 작업

- 실기기에서 `닉네임 수정`, `비밀번호 변경`, `기록하기`, `일정 추가`, `펫 프로필 수정`, `펫 등록 1/2`, `펫 등록 2/2`, 홈 스크롤 복귀를 재검증한 뒤 Step 3 auto-hide 증적 수집으로 복귀한다.

### 완료 ID

- `REL-2026-03-31-TASK-2-9-3-RECORDCREATE-SYNC`

### 작업명

- Task 2.9.3 `RecordCreate` keyboard 표준 이식

### 완료일

- 2026-03-31

### 무엇을 했는지

- `ScheduleCreateScreen`, `PetProfileEditScreen`, `PetCreateScreen`의 keyboard 대응 라이브러리를 `react-native-keyboard-controller`의 `KeyboardAwareScrollView`로 통일했다.
- 세 화면의 하단 여백식을 `Math.max(insets.bottom + 184, keyboardInset + 108, 300)`으로 고정했다.
- `펫 등록`에서 남아 있던 `scrollToFocusedInput`, `scrollToPosition` 의존을 제거하고 `assureFocusedInputVisible()`로 바꿨다.

### 검증 결과

- `yarn tsc --noEmit` 통과
- `yarn test --watchAll=false --watchman=false __tests__/authNotices.test.ts __tests__/appBoot.test.ts __tests__/authStoreRecovery.test.ts __tests__/passwordRecoverySession.test.ts` 통과
- `git diff --check` 통과

### 남은 리스크

- 이번 턴은 repo 기준 keyboard 계약 동기화다. Android 실기기에서 `일정 추가`, `펫 프로필 수정`, `펫 등록 1/2·2/2`는 다시 확인해야 한다.
- Step 3 auto-hide 증적은 아직 미완료다.

### 다음 연결 작업

- 실기기에서 세 화면의 keyboard/CTA 접근성을 다시 확인한 뒤 Step 3 auto-hide 증적 수집으로 복귀한다.

### 완료 ID

- `REL-2026-03-31-TASK-2-9-4-DYNAMIC-BOTTOM-INSET`

### 작업명

- Task 2.9.4 다이내믹 하단 여백 적용

### 완료일

- 2026-03-31

### 무엇을 했는지

- `RecordCreate`, `ScheduleCreate`, `PetProfileEdit`, `PetCreate`의 하단 `paddingBottom`을 고정 `300px`에서 다이내믹 공식으로 교체했다.
- 네 화면의 CTA는 계속 스크롤 마지막 자식으로 유지했고, 별도 `marginBottom`을 제거해 키보드 위 간격을 과하게 벌리지 않도록 정리했다.

### 검증 결과

- `yarn tsc --noEmit` 통과
- `yarn test --watchAll=false --watchman=false __tests__/authNotices.test.ts __tests__/appBoot.test.ts __tests__/authStoreRecovery.test.ts __tests__/passwordRecoverySession.test.ts` 통과
- `git diff --check` 통과

### 남은 리스크

- 이번 턴은 repo 기준 최적화다. Android 실기기에서 하단 공백이 충분히 줄었는지, 키보드 위 CTA 위치가 의도대로 20~40px 감각인지 다시 확인해야 한다.

### 다음 연결 작업

- 실기기에서 네 화면의 dynamic bottom inset 결과를 확인한 뒤 Step 3 auto-hide 증적 수집으로 복귀한다.

### 완료 ID

- `REL-YYYY-MM-DD-이름`

### 작업명

- 작업명

### 완료일

- YYYY-MM-DD

### 무엇을 했는지

- 실제로 닫은 변경과 범위를 적는다.

### 검증 결과

- 실행한 테스트, 명령, 수동 확인 결과를 적는다.

### 남은 리스크

- 아직 안 닫힌 운영/QA/문서 리스크를 적는다.

### 다음 연결 작업

- 바로 이어질 다음 작업 1개를 적는다.
