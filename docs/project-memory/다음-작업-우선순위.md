# 다음 작업 우선순위

## 지금 당장 할 것

### 1. 우리동네 동물병원 운영 처리량 확대

- 이유
  - 도메인 foundation, remote migration, service-role apply, Android 실기기 CTA/지도 증적까지는 확보됐다.
  - 현재 병목은 구조가 아니라 운영 처리량이다. approved phone은 3,024건까지 올라왔지만, approved coordinates 2건, approved thumbnail 2건, approved open24Hours 5건, approved exoticAnimalCare 2건이라 체감 coverage는 아직 낮다.
  - provider enrichment와 official-source import로 queue는 충분히 쌓였으므로, 다음 priority는 pending/held를 검수해 public CTA coverage를 실제로 올리는 것이다.
- 선행 조건
  - admin 계정으로 운영자 화면 직접 진입
  - 검수 UI approve/reject/held 조작 증적
  - held thumbnail 868건, pending coordinates 327건, pending open24Hours 43건 우선순위 검수
  - provider batch 추가 실행 전 quota와 review capacity 확인
  - 운영시간 문자열 전체를 public에 열려면 별도 source schema와 approved verification policy 확정

### 2. Guestbook -> private letters 전환

- 이유
  - GuestbookTab은 repo 기준 `편지함` 표시와 선택 펫 기준 private letters 작성/조회 UI로 1차 전환됐다.
  - 공개형 확장은 계속 금지 방향이며, 이번 구현도 user-authored private letter만 다룬다.
  - 이후 Premium AI reply 구조도 이 전환을 선행조건으로 가진다.
- 선행 조건
  - Android 실기기에서 편지 작성/목록 반영 smoke
  - linked remote `letters` table/RLS 실제 catalog 재확인
  - private letters 상세/수정/삭제 policy 결정
  - Premium AI reply 전 consent, entitlement, generation log 설계

### 3. typography foundation rollout

- 이유
  - AppText와 typography token은 있지만 실제 화면 소비가 분산돼 있어 전체 밀도와 브랜드 톤이 흔들린다.
  - foundation만 두고 미루면 이후 새 도메인 화면도 다시 raw text로 늘어날 가능성이 높다.
- 선행 조건
  - preset 재정의
  - 화면군 rollout 순서

### 4. social login 계약 정리

- 이유
  - 버튼은 이미 노출돼 있지만 실제 provider, callback, native 설정은 비어 있다.
  - 추후 로그인 진입 전환율을 올리려면 `Google first`, `Apple iOS 동시`, `Kakao 후행` 원칙을 먼저 확정해야 한다.
- 선행 조건
  - Supabase provider enablement
  - deep link / callback contract

### 5. entitlement / billing foundation

- 이유
  - Premium AI, 멀티펫 구독, 향후 paywall은 entitlement 없이 열면 안 된다.
  - 현재 문서 권장 구조와 remote 실제 구조가 불일치하므로 foundation을 먼저 맞춰야 한다.
- 선행 조건
  - `user_subscriptions`
  - `user_entitlements`
  - billing event contract

### 6. Premium AI reply 선행조건 정리

- 이유
  - 이 기능은 핵심 BM 후보지만, letters IA, entitlement, consent, generation log, provider policy가 먼저 닫혀야 한다.
  - AI 자체 구현보다 grounded personalization과 안전 규칙을 먼저 고정해야 한다.
- 선행 조건
  - private letters 구조
  - entitlement
  - AI notice / consent

## 이미 닫힌 항목

- 정책 문서 public 연결과 앱 복귀
- 비밀번호 재설정 복귀
- 계정 탈퇴 7일 유예와 자동 파기 worker 배포
- 커뮤니티 신고 정책, auto-hide, reporter flag trace, image cleanup pending 계약
- 닉네임 정책 2..10 정렬과 Android 레이아웃 보정
- 커뮤니티 인앱 정책 notice
- 건강관리 리포트 Phase 1 repo 구현, linked remote 반영, row-level 검증, Android 실기기 핵심 QA
- ESLint release gate error 5건 fix-only 정리
- 건강관리 전용 기록하기 1차 전환
- 타임라인 `건강` 신규 작성 선택지 제거와 기존 health 기록 read-path 유지
- 생활/일정 병원·약 신규 작성 진입점 이관과 일정추가 UI 보더/아이콘 보정
- 건강관리 인사이트 탭 일반 사용자용 월간 요약/그래프 전환

## 지금은 후순위로 두는 것

- release checklist 최종 마감
- evidence pack 최종 보관본
- 스토어 제출 자산
- RC smoke
- 모니터링/배포 최종 확인

## 왜 이 순서인가

- 지금은 출시가 아니라 구현 완성도가 먼저다.
- release lane은 삭제하지 않되, 모든 핵심 task가 닫힌 뒤 마지막 게이트로 미룬다.
- 먼저 해야 하는 것은 제품 구조를 실제 방향인 `병원 / private letters / entitlement / social auth / Premium AI 선행조건`으로 옮기는 일이다.
