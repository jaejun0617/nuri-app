# 다음 작업 우선순위

## 지금 당장 할 것

### Task 5. 장소/여행 Trust Layer 적용 계획 확정

- 이유
  - Task 4의 linked project remote blocker 3개가 hotfix와 재검증으로 닫혔고, 다음 실제 제품 신뢰도 리스크는 장소/여행 candidate 데이터를 trust layer 없이 계속 노출하는 것이다.
- 현재 상태
  - Task 2 구현 완료
  - Task 2.5 완료
  - Task 3 구현 완료
  - Task 4는 repo 기준 구현/운영 검증, remote hotfix, linked project 재검증까지 반영됐다.
  - linked project `grmekesqoydylqmyvfke` 기준으로 comment auto-hide, reporter flag trace, community image upload, `community_image_assets` backfill/attach, hidden 이미지 비노출, cleanup_pending 전환까지 다시 확인됐다.
  - 남은 것은 기능 blocker보다 release 직전 운영 캡처와 SQL/콘솔 SOP 정리다.
- 선행 조건
  - Task 4 remote 재검증 결과를 현재 문서와 release checklist에 반영
  - candidate/trust/user layer 분리 기준 재확인
  - 장소/여행 SQL 초안과 실제 런타임 비교
- 승인 필요 여부
  - 중간 이상
  - 문서 정리만이면 승인 없이 가능하지만, 런타임 구조나 DB 계약을 바꾸면 승인 필요다.

## 그 다음 할 것

### Release 직전 운영 캡처 보강

- 이유
  - Task 4 blocker는 닫혔지만, moderation queue/action log/cleanup_pending을 운영자가 SQL/콘솔 기준으로 어떻게 확인하는지 캡처와 절차를 남겨야 release 시 오판을 줄일 수 있다.
- 선행 조건
  - linked project 또는 staging/prod에서 auto-hide / cleanup_pending / restore 시나리오를 다시 캡처
  - queue/action log 확인 SQL과 cleanup_pending 처리 절차를 운영 기준으로 묶기
- 승인 필요 여부
  - 낮음
  - 문서와 캡처 보강 중심이다.

### 그 이후 병렬로 정리할 장기 운영 리스크

- 정책 문서 최종화
  - 이유: source of truth와 초안 구조는 이미 생겼지만, 법무/운영 기준의 최종 본문과 공개 URL은 아직 남아 있다.
- 외부 API 키 / 환경 분리
  - 이유: Kakao/TourAPI 키 노출과 dev/staging/prod 혼선 리스크는 Task 5 이후에도 별도 운영 과제로 남는다.
- 운영 도구 / 감사 체계
  - 이유: community queue/action log가 생겨도 전용 운영 UI와 감사 체계가 없으면 장기 운영 피로도가 계속 높다.

## 왜 이 순서인가

- 5차는 새 기능을 넓히는 단계가 아니라 배포 차단 리스크를 줄이는 단계다.
- Task 4 원격 blocker 3개는 닫혔고, 다음 본격 리스크는 장소/여행 trust layer 공백이다.
- 따라서 지금은 Task 5 설계를 시작하되, release 직전 운영 캡처와 SQL/콘솔 SOP 보강을 병렬로 유지하는 순서가 가장 안전하다.

## 지금 당장 하면 안 되는 것

- 소셜 로그인 선구현
  - 이유: 계정 복구와 병합 정책이 먼저다.
- 결제 선구현
  - 이유: 현재 5차 목표와 무관하고 운영 차단 리스크를 줄이지 못한다.
- Guestbook 기능 확장
  - 이유: placeholder를 기능 완성으로 포장하면 우선순위가 흐려진다.
- moderation 없는 커뮤니티 기능 확장
  - 이유: 공개 도메인 방어선이 먼저다.
- Candidate 데이터를 confirmed로 취급
  - 이유: 신뢰도 사고를 직접 유발한다.

## 현재 차단 및 확인 포인트

- 실제 정책 문서 URL 또는 앱 내 정책 화면의 최종 본문은 아직 확정되지 않았다.
- 정책 문서 source of truth는 `src/services/legal/documents.ts`로 정리됐고 초안 문서 구조도 생겼지만, 현재 상태는 여전히 `최종 확정 전`이다.
- 계정 삭제 SQL/RPC는 문서 기준으로 구현됐지만, staging/prod 실제 적용 확인과 cleanup worker 검증은 아직 남아 있다.
- storage orphan 정리 계약은 queue까지 구현됐고, 이제는 실제 재처리 운영 경로를 확인해야 한다.
- 비밀번호 재설정 구현은 들어갔지만, 실제 Supabase dashboard에 `nuri://auth/reset` redirect가 허용돼 있는지와 실기기 메일 복귀 QA는 아직 남아 있다.
- typography/accessibility 리팩토링은 끝났지만, Auth / GuestHome / CommunityDetail 기준의 200% 글꼴 확대와 긴 한글 CTA 실기기 QA는 아직 남아 있다.
- 커뮤니티 abuse 최소 방어선은 remote blocker까지 닫혔지만, 운영자가 SQL/콘솔 기준으로 queue/action log/cleanup_pending을 확인하는 절차 캡처는 더 보강할 수 있다.
- 장소/여행은 Candidate / Trust / User Layer 분리 원칙은 문서화됐지만, 런타임 source of truth는 아직 candidate 중심이다.
- 외부 API 키 노출/호출량 통제와 dev/staging/prod 분리는 여전히 후속 운영 과제로 남아 있다.

## 실기기 검증 대기 항목

- 회원가입에서 정책 링크 이동이 iOS/Android 모두 정상 동작하는지 확인
- More/계정 화면에서 삭제 안내 경로가 실제 문서와 정합적인지 확인
- 정책 문서 접근 실패 시 UX가 차단/안내 기준을 만족하는지 확인
- iOS/Android 메일 앱에서 `nuri://auth/reset` 복귀가 정상 동작하는지 확인
- reset 링크 만료/오류 시 `PasswordResetRequest` 복귀와 재발송 60초 쿨다운이 의도대로 동작하는지 확인
- Auth / GuestHome / CommunityDetail에서 200% 글꼴 확대와 긴 문장 줄바꿈이 깨지지 않는지 확인
