# 다음 작업 우선순위

## 지금 당장 할 것

### 1. Guestbook -> private letters 실기기/remote 확인

- 이유
  - GuestbookTab은 repo 기준 `편지함` 표시와 선택 펫 기준 private letters 작성/조회 UI로 1차 전환됐다.
  - 공개형 확장은 계속 금지 방향이며, 이번 구현도 user-authored private letter만 다룬다.
  - Premium AI reply 구조도 private letters 확인을 선행조건으로 가진다.
- 선행 조건
  - Android 실기기에서 편지 작성/목록 반영 smoke
  - linked remote `letters` table/RLS 실제 catalog 재확인
  - private letters 상세/수정/삭제 policy 결정

### 2. entitlement / billing foundation

- 이유
  - Premium AI, 멀티펫 구독, 향후 paywall은 entitlement 없이 열면 안 된다.
  - 현재 문서 권장 구조와 remote 실제 구조가 불일치하므로 foundation을 먼저 맞춰야 한다.
- 선행 조건
  - `user_subscriptions`
  - `user_entitlements`
  - billing event contract

### 3. Premium AI reply 선행조건 정리

- 이유
  - 이 기능은 핵심 BM 후보지만, letters IA, entitlement, consent, generation log, provider policy가 먼저 닫혀야 한다.
  - AI 자체 구현보다 grounded personalization과 안전 규칙을 먼저 고정해야 한다.
- 선행 조건
  - private letters 구조
  - entitlement
  - AI notice / consent

### 4. typography foundation rollout 여부 판단

- 이유
  - AppText와 typography token은 있지만 실제 화면 소비가 분산돼 있어 전체 밀도와 브랜드 톤이 흔들린다.
  - 다만 지금 단계가 release closeout이면 대규모 rollout은 리스크가 될 수 있으므로 적용 여부부터 판단한다.
- 선행 조건
  - release blocker와 직접 관련된 화면군인지 확인
  - rollout을 한다면 Auth, Home, Location, Modal 같은 제한된 화면군부터 진행

### 5. clean RC artifact build/evidence 고정

- 이유
  - 2026-04-30 RC smoke는 dirty working tree와 설치 앱 기준으로 고정됐다.
  - 최종 제출 전에는 clean RC artifact, commit/branch 상태, 설치 APK provenance, version manifest를 evidence로 고정해야 한다.
  - 이 항목만 현재 P1 release evidence blocker로 남긴다.
- 선행 조건
  - clean working tree 또는 명시 승인된 release branch
  - release candidate build artifact 확정
  - 동일 package/version manifest 확인

### 6. 전역 운영자 QA final gate

- 이유
  - 동물병원 운영자 검수 UI approve/reject/held 실계정 QA는 동물병원 도메인 작업이 아니라 전역 operator final gate다.
  - 모든 주요 도메인 closeout 이후 마지막에 수행하거나, PO가 별도 보류 승인한다.
- 선행 조건
  - `docs/qa/admin-qa-backlog.md` 기준으로 final gate 대상 확정
  - 운영자 계정과 실계정 QA 범위 확정

### 7. 스토어 출시 자산 세팅

- 이유
  - 스토어 출시 자산 세팅은 최종 제출 직전 task로 이동한다. 현재 단계에서는 기능/QA/evidence closeout이 우선이다.
  - 앱 아이콘, 스플래시, 스크린샷, 스토어 문구는 RC smoke와 final gate 상태가 정리된 뒤 확정한다.
- 선행 조건
  - release checklist 후행 캡처 완료
  - RC smoke 완료
  - 남은 release blocker 정리
  - Guestbook/private letters 실기기/remote 확인
  - 전역 운영자 QA final gate 수행 또는 보류 승인
  - 최종 evidence pack 정리

### 8. 스토어 제출 직전 최종 캡처/메타데이터 정리

- 이유
  - 최종 제출 직전에는 실제 제출 빌드 기준 스크린샷, 정책 URL, 문의처, 설명문, 버전 메타데이터가 서로 일치해야 한다.
  - 이 단계는 스토어 자산 세팅 이후 마지막 release packaging 작업이다.
- 선행 조건
  - 스토어 출시 자산 세팅 완료
  - 최종 제출 빌드 확정
  - 정책 URL과 지원 연락처 최종 확인

## 이미 닫힌 항목

- 정책 문서 public 연결과 앱 복귀
- 비밀번호 재설정 복귀
- 계정 탈퇴 7일 유예와 자동 파기 worker 배포
- 커뮤니티 신고 정책, auto-hide, reporter flag trace, image cleanup pending 계약
- 닉네임 정책 2..10 정렬과 Android 레이아웃 보정
- 커뮤니티 인앱 정책 notice
- 건강관리 리포트 Phase 1 repo 구현, linked remote 반영, row-level 검증, Android 실기기 핵심 QA
- ESLint release gate error 5건 fix-only 정리
- 건강관리 전용 기록하기 1차 전환
- 타임라인 `건강` 신규 작성 선택지 제거와 기존 health 기록 read-path 유지
- 생활/일정 병원·약 신규 작성 진입점 이관과 일정추가 UI 보더/아이콘 보정
- 건강관리 인사이트 탭 일반 사용자용 월간 요약/그래프 전환
- 동물병원 유저 서비스 기능 기준 close. 운영자 검수 UI approve/reject/held 실계정 QA는 전역 operator final gate로 분리한다.
- 날씨 도메인 비용 방어: `weather-cache` remote deploy, `nuri_weather_cache` remote apply, Android 실기기 홈/상세 QA, Open-Meteo client direct call 0건 검증
- social login v1.0 app-side 구현: Kakao/Google 버튼은 Supabase OAuth web flow에 연결했고, callback은 `nuri://auth/callback`으로 password reset `nuri://auth/reset`과 분리했다.
- social login provider 설정 대기: Google/Kakao 콘솔/API key/secret/redirect allow-list와 OAuth 성공 smoke는 PO 설정 체크 턴에서 닫는다. 실제 secret 값은 repo에 기록하지 않는다.
- Release Evidence Pack hard-close: Android RC smoke 조건부 통과, linked remote migration/lint/functions 확인, P0 없음, P1은 clean RC artifact 기준 evidence 고정 1건으로 제한

## 지금은 후순위로 두는 것

- Naver/Apple social login backlog
  - Naver는 v1.1 이후 후순위다. Supabase custom OAuth/OIDC 또는 별도 callback 구성이 필요할 수 있다.
  - Apple은 Android-first v1.0 범위에서 제외하고 iOS 출시 및 Apple 정책 검토 시점에 별도 판단한다.
- 스토어 출시 자산 세팅
- 스토어 제출 직전 메타데이터 정리
- Open-Meteo customer API key/계약 전환
- `weather-cache` public no-JWT endpoint abuse throttle/rate limit
- place enrichment stale cache refresh 고도화

## 왜 이 순서인가

- 날씨와 동물병원은 v1.0 기준 close됐으므로 다시 도메인 P1로 열지 않는다.
- release checklist 후행 캡처, RC smoke, 남은 blocker 정리는 evidence pack으로 hard-close됐다.
- social login app-side P1은 Google/Kakao OAuth entrypoint와 callback route 구현으로 닫혔다. provider 콘솔 설정과 OAuth 성공 smoke는 PO 설정 대기 항목으로 분리한다.
- provider 설정 체크가 별도 턴에서 닫히면 다음 제품 작업은 Guestbook -> private letters 확인이다.
- 스토어 자산은 제출 직전 상태가 고정된 뒤 다루는 것이 안전하다.
