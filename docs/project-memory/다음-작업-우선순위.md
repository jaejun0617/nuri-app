# 다음 작업 우선순위

## 운영 원칙

- v1.0은 기존 기능을 안정화해서 출시한다.
- v1.0은 스토어 제출 완료 버전이 아니라 기능 개발 Code Freeze 기준선이다.
- v1.1은 v1.0 미완성 이월이 아니라 신규 업데이트 트랙이다.
- v1.0 잔여 task와 release risk는 P0/P1 기준 닫혔다. 이제 V1.1 신규 업데이트 트랙을 시작할 수 있다.
- 운영자 QA, 실기기 최종 스모크, 최종 RC build artifact/provenance는 2026-06-02 기준 수행됐다. Provider console/OAuth smoke는 Google/Kakao 완료와 Naver soft disable로 V1.0에서 닫혔다.
- 2026-06-02 release APK exact install smoke는 기존 debug 설치본 uninstall 후 성공했다. 설치된 base APK SHA-256은 artifact와 동일하고 NURI Upload certificate 서명, non-debuggable 설치본으로 확인됐다.
- 2026-06-02 일반 사용자 final smoke와 동물병원 admin/super_admin 서버 조작 QA는 통과했다.
- 2026-06-04 `profiles.role` self-escalation P0는 corrective migration과 remote/release 앱 회귀 테스트로 닫혔다.
- Play Store 제출 자산은 V1.0 기능/QA blocker가 아니며, NURI 앱 개발과 QA가 완전히 끝난 뒤 최종 제출 직전 준비 단계에서 진행한다.
- 2026-06-11 기준 전체 프로젝트 현황 보고서와 아버지 공유용 사업 진행 보고서/예산서를 `docs/reports/`에 생성했다. 다음 개발 실행 우선순위는 변하지 않으며, V1.1 서울 2차 산책 POI coverage batch와 admin write UI/action log 운영 증적 고도화가 다음 실행 단위다.
- 2026-06-15 정합성 검수 기준 서울 1차 seed 33건, 총 approved/public/active POI 86건, admin write UI e2e/action log drill-down은 실제 remote/code 기준으로 맞다.
- 2026-06-15 서울 2차 seed 44건을 admin import/review workflow로 승인했고 총 approved/public/active POI는 130건이다. 서울 전체 fallback gate는 아직 금지하고, 월드컵공원/난지/망원, 반포/잠원/이촌, 뚝섬/서울숲 3개 coverage region에만 gate를 추가 적용한다.
- 2026-06-15 서울 보류 권역 seed 67건을 admin import/review workflow로 승인했고 총 approved/public/active POI는 197건이다. 송파/올림픽공원/석촌호수, 양재천/탄천, 중랑천, 안양천, 보라매/도림천에 fallback gate를 추가 적용하고, 북서울꿈의숲은 5km 기준 미달로 유지한다.
- 2026-06-15 seed 한글화 정규화와 북서울꿈의숲/수도권 1차 seed 48건을 admin import/review workflow로 승인했고 총 approved/public/active POI는 245건이다. 북서울꿈의숲과 성남·분당·판교·탄천은 gate 기준을 충족해 fallback gate를 추가 적용한다. 서울 전체와 수도권 전체 gate는 계속 금지한다.
- 2026-06-16 수도권 2차 seed 60건을 admin import/review workflow로 승인했고 총 approved/public/active POI는 305건이다. 하남·미사한강공원, 수원·광교호수공원, 과천·서울대공원은 3km/5km coverage 기준을 충족해 fallback gate를 추가 적용한다. 서울 전체/수도권 전체/전국 전체 gate는 계속 금지한다.
- 2026-06-17 수도권 3차 seed 80건과 전국 주요 도시 1차 seed 80건을 admin import/review workflow로 승인했고 총 approved/public/active POI는 465건이다. 인천 송도, 부천 상동, 안양·학의천·안양천, 남양주·다산·왕숙천, 부산 해운대·동백섬, 대구 수성못, 대전 갑천·엑스포, 울산 태화강 국가정원은 gate 기준을 충족해 fallback gate를 추가 적용한다. 서울 전체/수도권 전체/전국 전체 gate는 계속 금지한다.
- 2026-06-19 전국 seed 2차 140건과 수도권 잔여 seed 80건을 admin import/review workflow로 승인했고 총 approved/public/active POI는 685건이다. 광주, 세종, 청주, 천안, 춘천, 강릉, 제주, 용인·기흥, 군포 초막골, 시흥 갯골, 김포 한강신도시는 gate 기준을 충족해 fallback gate를 추가 적용한다. 서울 전체/수도권 전체/전국 전체 gate와 Kakao Local runtime code delete는 계속 금지한다.
- 2026-06-20 Kakao Local runtime closeout 1단계로 Ready 권역의 POI RPC 정상 응답 result 있음/0건 조건에서 Kakao fallback 호출을 차단했다. focused test는 Ready result 있음 Kakao 호출 0, Ready empty Kakao 호출 0, gate 밖 fallback 유지, RPC error fallback 유지, 좌표 없음 fallback 유지, feature flag off fallback 유지를 고정한다. hard delete는 아직 금지하고, gate 밖/좌표 없음/RPC error/detail missing/feature flag off는 계속 Keep Fallback으로 유지한다.
- 2026-06-20 hard delete closeout 최종 점검 결과, Kakao Local hard delete는 보류한다. 산책 Ready 권역의 Kakao 호출 차단은 유지하지만, Kakao Local provider는 pet-friendly 검색, 동물병원 provider matching, 행정동 조회와 공유된다. 다음 hard delete 작업은 shared provider 분리 설계가 먼저다.
- 2026-06-21 전국 seed 3차 180건과 전국 seed 4차 120건을 admin import/review workflow로 승인했고 총 approved/public/active POI는 985건이다. batch는 `badb210c-3cb3-4643-a812-032222d415b4`, `b8fc90fa-7680-48ba-9cdf-d7638b4a2c7e`이며, 전주/창원/포항/김해/여수/순천/목포/구미/진주와 부산 온천천, 대구 신천, 대전 유림공원, 울산 선암호수공원, 경주 보문호, 군산 은파호수공원 권역을 보강했다. 신규 15개 권역은 3km/5km coverage와 focused fallback gate test를 통과해 권역별 gate만 추가 적용하고 전국 전체 broad gate는 계속 금지한다.
- 2026-06-21 전국 seed 5차 160건을 admin import/review workflow로 승인했고 총 approved/public/active POI는 1,145건이다. batch는 `3f345ae7-e8a6-404b-a5b5-d7f555fb75b0`이며, 마산·진해, 통영, 거제, 안동, 익산, 나주, 사천, 양산 각 20건을 보강했다. 신규 8개 권역은 3km/5km/search coverage와 focused fallback gate test를 통과해 권역별 gate만 추가 적용한다. 전국 전체, 수도권 전체, 서울 전체, 도시 전체 broad gate는 계속 금지한다.
- 2026-06-22 최종 closeout audit 기준 산책 POI 트랙은 `closeout 가능`으로 판정한다. remote read-only 기준 approved/public/active POI 1,145건, public RPC 정상, non-approved public active leak 0건, public RPC internal key leak 0건, 한글 alias 누락 0건, source/attribution 누락 0건, broad gate 오적용 0건, Android `SM_S937N` 리스트/상세/empty safe UX smoke를 확인했다. admin UI 운영자 QA closeout은 앱 출시 blocker가 아니므로 Parking하고, 별도 홈페이지/관리 페이지 트랙으로 이동한다.
- 2026-06-23 release candidate smoke와 운영비 readiness 점검을 수행했다. release APK update install은 성공했고, 홈/Auth 버튼/Profile/Pet/Health/Animal Hospital/Walk/Community/Timeline/Weather/전체메뉴/로그아웃 복귀 smoke는 crash 없이 통과했다. 산책 POI public RPC와 projection negative smoke도 유지됐고, logcat fatal / ANR / unhandled promise / ReactNativeJS fatal pattern은 0건이다. Supabase 실제 plan과 Codex 실제 플랜은 dashboard/account owner 확인이 필요하다.
- 2026-06-30 신규 QA 계정 full E2E/navigation audit와 Animal Hospital 전국 coverage read-only audit을 수행했다. 로그아웃 후 email/password 재로그인 stale onboarding blocker를 최소 수정했고, release APK rebuild/install 후 cold start와 재로그인 home 복귀를 재검증했다. 주요 사용자-facing 화면 17개 back audit은 통과했고, 병원 public active 5,427건과 대표 권역 coverage도 확인했다. 디자인 수정은 하지 않았고 스토어 출시 전 디자인 조정 후보를 별도 트랙으로 둔다.
- 2026-06-30 추가 read-only audit 기준 Animal Hospital coordinate missing 122건은 release blocker가 아니다. 122건은 public visible active에 포함되지만 nearby 좌표 기반 리스트에서는 제외되고, text search/detail에서는 주소/전화 정보형 표시와 좌표 없음 fallback으로 안전 처리된다. 병원 전국 판정은 `우리동네 병원 찾기 전국 확장 완료, coordinate missing 122건은 release blocker 아님`으로 갱신한다.
- 2026-06-30 PO 확정 기준 Supabase/Codex 운영비는 PO 확정 완료 상태다. 비용 확인을 다음 액션으로 반복하지 않고, V1.1 추가 업데이트 1차 MVP와 정책 확정을 다음 실행 단위로 둔다.
- 2026-06-30 V1.1 추가 업데이트 1차 MVP edge QA를 닫았다. 회원탈퇴 입력 확인, 최근 로그인 표시, 타임라인 카테고리 count는 typecheck/lint/focused test를 통과했고, Android에서는 회원탈퇴 모달 disabled/cancel/back/7일 유예 안내, email 최근 로그인 cold start 유지, Kakao callback, Google chooser/redirect, 타임라인 작성/카테고리 수정/삭제 count 갱신을 확인했다. 실제 탈퇴 예약과 social 최종 pill은 QA 계정 보호와 외부 OAuth/입력 자동화 제약 때문에 조건부 evidence로 둔다.
- 2026-07-01 V1.1 추가 업데이트 1차 MVP는 `조건부 closeout 유지`로 재판정한다. release blocker는 없고, 실제 탈퇴 예약과 social 최종 pill만 조건부 evidence로 남긴다. 모든 실기기 QA에는 키보드바/입력창 가림/primary action 접근성/Android back keyboard dismiss/모달 크기 확인을 필수로 포함한다.
- 2026-07-01 V1.1 추가 업데이트 2차 MVP를 구현했다. 데일리 streak는 KST 00:00과 user+pet 하루 1회 인정, XP/칭호는 server ledger와 daily cap/source idempotency, 알림 read path는 전체메뉴 알림함/목록/읽음 처리 MVP를 기준으로 닫았다. 운영자 발송 UI, push, 홈 위젯, 무지개다리 서비스는 열지 않았다.
- 2026-07-01 V1.1 추가 업데이트 2차 MVP edge QA를 `adminQA` 일반 사용자 기준으로 닫았다. KST 다음날/missed day streak, XP daily cap/idempotency/title 중복 방지, 알림 unread/mark read/user isolation, RLS/RPC negative smoke, Android release APK/keyboard bar smoke가 통과했다.
- 2026-07-02 홈 알림 UX closeout 기준, 이전 인라인 알림 패널은 PO 의도와 달리 홈 콘텐츠를 밀어내는 구조라 제거했다. 홈 상단 알림 아이콘은 `오늘의 메시지로 하루를 시작해요` 문구 아래 위치에 상단 floating notification shade overlay를 열며, X/backdrop/Android back으로 닫는다. 실기기 `SM_S937N / R5CY613NMSY`에서 overlay screenshot/uiautomator 증적, open 전후 홈 콘텐츠 y좌표 유지, 다중 알림 내부 스크롤, 높아진 overlay panel, 카드 이동형 좌우 스와이프 dismiss, 알림별 X 제거, 화살표-only 펼침/접힘, 전체삭제 후 empty state를 확인했다. push, 운영자 발송 UI, 홈 위젯형 알림 카드는 후속 트랙으로 유지한다.
- 2026-07-02 V1.1 final sign-off 기준 V1.0 회귀, V1.1 산책 POI, V1.1 1차/2차 MVP, notification 최신 UX, RLS/RPC, Android `adminQA` smoke를 재검증했고 `V1.1 final sign-off 가능`으로 판정한다. V1.1.1 후보 scope audit에서 Android 홈 위젯 native/JS 일부 코드가 release build에 남아 있음을 확인해 receiver disabled/exported false와 native package 미등록으로 release 노출을 막았다. 새 기능 구현, DB/migration/seed 변경, push/운영자 발송 UI/무지개다리/고급 랭킹 구현은 하지 않았다.
- 2026-07-02 V1.1.1 후보 우선순위는 고급 XP/칭호/훈장/활동내역 대시보드를 1순위로 확정했다. 이번 턴에서 `전체메뉴 > 나의 반려동물 > 활동·칭호` 화면을 추가했고, 기존 XP ledger/RLS/RPC를 재사용해 현재 성장, 펫별 활동, 산책/타임라인/건강관리, user-level 공통 커뮤니티/댓글, 칭호·훈장 보관함을 표시한다. push, 운영자 발송 UI, 홈 위젯, 무지개다리, 고급 랭킹/리더보드는 구현하지 않았다.
- 2026-07-03 V1.1.1 1차 closeout에서 홈 간편 알림창의 swipe dismiss/`모두 치우기`를 전체보기 delete와 분리했다. 홈에서는 local/user-scoped hide만 적용하고, 전체보기 개별 삭제/전체삭제만 서버 RPC의 user-scoped hide로 유지한다. `adminQA` 실제 산책 글 write smoke로 활동·칭호 총 XP, 산책/타임라인/카테고리 카드, 공통 커뮤니티/댓글 카드, 획득/잠금 칭호 반영을 확인했다.
- 2026-07-03 조건부 closeout 해소 기준 `adminQA`에 QA 전용 `AdminQAPet2`를 추가해 실제 멀티펫 edge를 확인했다. 산책 2건, 식사 1건, 일기장 1건, 생활 1건 다중 write로 활동·칭호 총 `120 XP`, AdminQAPet2 카테고리 `전체 5 / 산책 2 / 식사 1 / 일기장 1 / 생활 1`, XP daily cap, owner label 칭호·훈장을 확인했다. 새 알림 row live retention은 안전 생성 경로 부재로 조건부 evidence로 남기며 service role key는 요구/노출하지 않았다.
- 2026-07-09 V1.1.1 고도화 1차로 운영자 알림 발송 관리 체계의 DB/RPC/RLS/audit 기반, QA 전용 알림 생성 RPC, Lv.1~30 확장, 장기 summary RPC, 전체메뉴 `누리 랭킹` MVP를 구현했다. 실제 원격 push 발송, 앱 내부 운영자 발송 UI, 홈 위젯, 무지개다리, Kakao Local global hard delete는 열지 않았다.
- 날씨와 동물병원처럼 이미 PO lock-in 된 도메인은 v1.0 blocker가 새로 확인되지 않는 한 다시 열지 않는다.
- 지도/API 비용 방어는 V1.0에서 provider runtime 차단으로 마감한다. 지도 엔진 교체, 자체 타일, 자체 POI DB는 V1.1 후보로만 다룬다.
- V1.0에서는 지도/API 비용 폭탄 방어 gate를 닫았다. Google Places/Photos 경로는 차단되었고, Kakao Local은 클라이언트 직접 호출 없이 서버 경유·캐시·fan-out 제한 상태로 통제한다. 단, Kakao Local은 provider-zero가 아니므로 V1.1에서는 산책/location discovery를 자체 POI DB + Supabase PostGIS 기반 반경 검색으로 전환한다. Kakao Local은 사용자 runtime에서 제거하고, 필요 시 admin seed 보조 도구로만 제한한다.

## 공통 완료 보고 규칙

앞으로 모든 Codex 작업 완료 보고에는 아래 프로젝트 진행률 블록을 포함한다.

- V1.0 기능 개발
- V1.0 QA/출시 준비
- V1.1 산책 POI 트랙
- V1.1 전체
- 전체 제품 로드맵
- 이번 작업으로 증가한 진행률
- 다음 작업 완료 시 예상 진행률
- 진행률 산정 근거

현재 기준 진행률은 V1.0 기능 개발 100%, V1.0 QA/출시 준비 약 99%, V1.1 산책 POI 트랙 약 99%, V1.1 추가 업데이트 기획 100%, V1.1 추가 업데이트 1차 MVP 약 98%, V1.1 추가 업데이트 2차 MVP 100%, V1.1 추가 기능 구현 약 94%, V1.1 전체 약 82%, V1.1.1 1차 기능 100%, V1.1.1 고도화 1차 100%, 전체 제품 로드맵 약 99.1%, 최종 제출 준비 약 20%다. 문서 정합성 보정처럼 구현 진척이 없는 작업은 진행률 증가를 0%로 보고한다.

## Android 실기기 자동 진행 원칙

2026-06-21부터 Galaxy 멀티윈도우 보존은 별도 완료 보고 필수 항목이 아니다. 실기기 QA에 필요한 NURI 앱 실행/재실행, release APK update install, deep link, `adb input`, `uiautomator dump`, logcat 수집, 한글 입력 우회를 위한 ASCII alias/좌표 override/RPC smoke는 사용자 중간 승인 없이 수행한다. 단, NURI 외 개인 앱 데이터 삭제, 기기 초기화, 계정 로그아웃, Play Console 제출, 결제/요금제 변경, 소셜 로그인 provider 변경, Kakao Login/Google Login 변경, `LoggedInHome.tsx` 수정, production destructive delete는 자동 승인 대상이 아니다.

## 현재 다음 작업

1. 관리자 홈페이지 본구현: 알림 발송/운영자 QA/신고/동물병원 검수/사용자 관리 기능을 실제 운영 콘솔로 확장
   - 기준: PO 정정에 따라 실제 관리자 홈페이지는 별도 `nuri-web` 프로젝트의 `/admin` 트랙이다. 앱 repo의 `admin-console/index.html`은 QA/임시 콘솔 및 레이아웃 참고 자산으로만 본다. 다음 단계는 `nuri-web`에서 auth/role gate, HTTPS 배포 전략, 실데이터 read-only 카드, audit feed, 알림 발송 console, 신고/삭제 moderation, 유저/펫 상세 관리를 연결하는 것이다.
   - 제외: 앱 내부 일반 사용자 admin UI, service role key 노출, production 전체 broadcast 즉시 활성화.
2. 앱 폰트/디자인 전체 리뉴얼: PO 승인 후 별도 턴에서 진행
   - 기준: 기능/QA blocker는 현재 없으나, 스토어 출시 전 전체 화면의 font scale, heading hierarchy, 카드 간격/radius/shadow, empty/loading/error tone을 한 번에 정리해야 한다.
   - 제외: Play Store 자산 생성, Play Console 입력, IA 대형 변경.

## 2026-07-11 최신 우선순위

현재 V1.1.1 1차의 `활동·칭호` 대시보드, 프리미엄 보상 모달, 운영자 알림 관리 콘솔 1차, 새 알림 live retention visual smoke, `누리 랭킹`, Lv.100 성장 정책, 홈 대표 칭호 badge, Home/domain progressive loading polish, 기록/일정 disk cache, ranking React Query 전환은 release blocker 없이 닫힌다. 병원/산책 direct visual smoke 조건부 항목도 Android release build 증적으로 닫혔다. 관리자 홈페이지는 앱 repo 정적 `admin-console`이 아니라 별도 `nuri-web` `/admin`을 실제 source of truth로 두고, 이번 리디자인으로 운영 도메인 전체 IA와 reference layout 기반 dashboard foundation을 잡았다. Play Store 자산 패키지는 아직 진행하지 않으며, 모든 기능 안정화, 관리자 홈페이지 본구현, 앱 전체 디자인 재정비, 최종 QA 이후로 둔다.

1. 앱 폰트/디자인 전체 리뉴얼: 실서비스급 안정화 완료 후 착수
   - 이유: 기능 안정화와 성능 안정화가 우선이었고, 이제 출시 전 첫인상 품질을 앱 전체 화면 기준으로 맞출 차례다.
   - 범위: font scale, heading hierarchy, 카드 간격/radius/shadow, empty/loading/error tone, Android 작은 화면/긴 텍스트 대응.
   - 제외: 기능 IA 대형 변경, Play Store 자산 생성, Play Console 입력.
2. 관리자 홈페이지 본구현: 알림 발송/운영자 QA/관리 기능을 별도 웹으로 구현
   - 이유: 실제 관리자 홈페이지는 `nuri-web`으로 분리되어 있고, 실운영에는 유저/펫/게시글/삭제/공지/병원/산책/QA를 한 곳에서 다루는 인증/권한/배포/감사/운영자 UX가 필요하다.
   - 범위: 운영자 로그인/권한 guard, 유저/펫 상세, 알림 발송 관리, 게시글 삭제/숨김 moderation, audit feed, QA/운영 대상 정책, 향후 push opt-in 연동 준비.
   - 제외: 일반 앱 내부 admin UI, production 전체 broadcast 즉시 활성화, push 실제 발송.

push token/opt-in 트랙은 관리자 홈페이지 본구현 또는 디자인 리뉴얼과 병행 가능하지만, 실제 remote push 발송과 FCM/Expo secret 사용은 별도 승인 후 진행한다. Play Store 자산 패키지는 위 안정화/디자인/관리자 홈페이지/최종 QA 이후에만 다음 액션으로 올린다.

## Parking / reference: 산책 POI 실행 히스토리

### 1. V1.1 1순위 마일스톤: 산책/location discovery 자체 POI DB + Supabase PostGIS 전환

- v1.0은 기능 개발 Code Freeze 기준선으로 고정됐다.
- v1.0은 스토어 제출 완료 버전이 아니라 기능 기준선이다.
- v1.1은 v1.0 잔여 task와 release risk가 닫힌 뒤 착수하는 신규 업데이트 트랙이며, 현재 착수 가능 상태다.
- `docs/qa/v1.0-remaining-task-risk-ledger.md` 기준 P0는 0건, P1은 0건이다.
- V1.0 필수 release blocker는 없다. Play Store 제출 자산은 최종 제출 직전 준비로 분리하고, V1.1 첫 개발 마일스톤에는 포함하지 않는다.
- P1 1순위의 console setup guide와 보안/API 방어 기준은 `docs/auth/social-provider-console-setup-guide.md`로 고정됐다.
- 2026-05-28 PO 최종 결정 기준 V1.0 소셜 로그인 provider는 Google + Kakao만 사용한다.
- Android 실기기 `R5CY613NMSY` / `SM_S937N`에서 Google은 web flow, 앱 복귀, session 생성, 기존 사용자 홈 진입까지 성공했다.
- Android 실기기에서 Kakao는 web flow, 앱 복귀, session 생성, 신규 사용자 `NicknameSetup`, 닉네임 저장, `PetCreate`, 펫 등록, 홈 진입까지 성공했다.
- Naver는 버튼/entrypoint를 V1.0 public surface에서 soft disable한다. Supabase `custom:naver` provider와 `signInWithNaver()` 코드는 hard delete하지 않는다.
- PetCreate/펫 프로필 수정 날짜 모달은 오래된 생일/입양일을 빠르게 넣을 수 있도록 `YYYY-MM-DD` 직접 입력을 제공한다. Android 실기기에서 `2010-05-12` 입력, invalid date 차단, 홈 카드 `생년월일 2010.05.12` 반영을 확인했다.
- 2026-05-29 final RC evidence baseline은 `docs/qa/final-rc-evidence-2026-05-29.md`로 고정했다.
- 2026-06-02 exact release APK 설치 smoke, 일반 사용자 final smoke, 동물병원 admin/super_admin 서버 조작 QA는 닫혔다.
- 2026-06-04 `profiles.role` self-escalation corrective migration과 회귀 테스트까지 닫혔다.
- V1.1 전체 작업 리스트와 추천 우선순위는 `docs/project-memory/v1.1-roadmap-briefing-2026-06-04.md`에 고정했다.
- 2026-06-04 `20260604110000_walk_poi_postgis_foundation.sql`을 linked remote에 적용했다. `walk_pois` canonical, source/import/review/audit/alias tables, public search/nearby/detail RPC, admin dry-run gate가 추가됐다.
- `anon` public search RPC는 seed 0건 상태에서 정상 반환했고, `anon` direct table SELECT는 permission denied, 비관리자 admin dry-run RPC는 `WALK_POI_ADMIN_REQUIRED`로 거부됐다.
- 2026-06-04 두 번째 단계에서 앱 산책/location discovery read path를 자체 POI public RPC 우선 + 기존 Kakao Edge Function fallback 병행 구조로 전환했다.
- remote에는 Android smoke용 approved operator seed 3건을 추가했다. 최종 public seed는 Android 실기기 현재 위치 기준 `문화공원 오거리공원`, `강선공원`, `일산호수공원`이며, 1차 서울시청 fallback seed는 public projection에서 제외했다.
- 2026-06-05 세 번째 단계에서 admin import commit/review workflow를 remote에 적용했다. `walk_poi_admin_import_commit_v1`은 hidden/pending candidate/source/review/audit을 만들고, `walk_poi_admin_review_v1`은 approve/reject/held를 처리한다. approved/public/active row만 public RPC에 노출되며 비관리자/anon admin 경로는 차단된다.
- 2026-06-05 네 번째 단계에서 일산/주엽/호수공원 생활권 approved seed 17건을 admin import commit/review workflow로 추가 승인했다. 반경 5km public nearby 결과는 starter seed 포함 20건이고, alias `ilsan0605` public search는 17건을 반환한다.
- Kakao Local fallback 축소 gate는 반경 3km 10건 이상, 반경 5km 20건 이상, 주요 검색어 hit-rate 70% 이상, nearby empty 20% 이하, fallback 호출 비율 30% 이하, POI RPC error rate 1% 이하, public projection 누수 0건으로 고정했다.
- 2026-06-05 다섯 번째 단계에서 coverage region 안의 `poi_empty`는 Kakao fallback으로 넘기지 않고 자체 POI empty UX로 닫는 gate를 적용했다. POI disabled, 좌표 없음, RPC error, detail missing, coverage region 밖은 기존 fallback을 유지한다.
- admin/super_admin 전용 `산책 POI 운영` read-only UI와 `walk_poi_admin_read_summary_v1` summary RPC를 추가했다. import batch, review queue, audit log, coverage/fallback gate, 전국 확장 준비 상태를 읽기 전용으로 확인할 수 있고 write action UI는 없다.
- 전국 POI 확장 로드맵은 `docs/domains/산책-위치기반-기능/v1.1-walk-poi-national-expansion.md`에 고정했다. 전국 대량 import는 아직 금지하고, 다음 실행 단위는 고양시 전체 seed coverage batch 확장이다.
- 2026-06-06 여섯 번째 단계에서 고양시 1차 seed coverage batch를 admin import/review workflow로 반영했다. `osm` batch `1d6bf293-c51d-4c6b-bdb8-74eec00989d6`로 신규 approved seed 33건을 승인했고, 총 approved/public/active POI는 53건이다.
- coverage gate는 일산/주엽/호수공원과 백석/마두/정발산에 적용한다. 화정/행신/삼송/원당은 3km/5km coverage와 검색 hit가 부족하므로 Kakao fallback을 유지한다.
- `산책 POI 운영` 화면에는 pending 후보 approve/reject/held 최소 write UI를 추가했다. import commit UI, raw payload, provider token 노출은 열지 않았다.
- 2026-06-06 일곱 번째 단계에서 서울 주요 산책 권역 1차 batch를 admin import/review workflow로 반영했다. `osm` batch `eff5b9af-3fba-447b-8c93-1d48dedc923c`로 신규 approved seed 33건을 승인했고, 총 approved/public/active POI는 86건이었다.
- 2026-06-15 서울 주요 산책 권역 2차 batch를 admin import/review workflow로 반영했다. `operator-seed` batch `f2e96855-43af-4da9-98da-f182ab030129`로 신규 approved seed 44건을 승인했고, 총 approved/public/active POI는 130건이다.
- 서울 2차 batch 후 월드컵공원/난지/망원, 반포/잠원/이촌, 뚝섬/서울숲은 3km 10건/5km 20건 기준을 충족해 fallback gate를 추가 적용했다. Android 실기기 `SM_S937N`에서 Galaxy 멀티윈도우를 유지한 채 `하늘공원` 리스트/상세, `9999` empty state, `seoul_worldcup_nanji_mangwon` gateLimited logcat을 확인했다. 서울 전체와 송파/올림픽공원/석촌호수 등 보류 권역은 fallback을 유지한다.
- admin write UI는 실제 admin/super_admin 세션에서 approve/reject/held button tap e2e와 action log drill-down까지 확인했다. `walk_poi_admin_audit_detail_v1`는 safe audit detail만 반환하고 raw payload/provider token/secret은 노출하지 않는다.
- 2026-06-15 서울 보류 권역 batch를 admin import/review workflow로 반영했다. `operator-seed` batch `b4a09762-dfff-4191-b93e-e8debdf63eac`로 신규 approved seed 67건을 승인했고, 총 approved/public/active POI는 197건이다.
- 서울 보류 권역 보강 후 송파/올림픽공원/석촌호수, 양재천/탄천, 중랑천, 안양천, 보라매/도림천은 3km 10건/5km 20건 기준을 충족해 fallback gate를 추가 적용했다. 북서울꿈의숲은 3km 12건/5km 19건으로 fallback을 유지한다.
- 2026-06-15 seed 한글화 정규화와 북서울꿈의숲/수도권 1차 seed batch를 admin import/review workflow로 반영했다. `operator-seed` batch `b6764de4-6a8c-4fb6-a1dd-998e6195379b`로 신규 approved seed 48건을 승인했고, 총 approved/public/active POI는 245건이다.
- 북서울꿈의숲은 3km 20건/5km 27건, 성남·분당·판교·탄천은 3km 29건/5km 40건으로 기준을 충족해 fallback gate를 추가 적용했다.
- 2026-06-16 수도권 2차 seed batch를 admin import/review workflow로 반영했다. `operator-seed` batch `d03e7cef-fb93-4b45-9e61-ec0c926952da`로 하남·미사한강공원, 수원·광교호수공원, 과천·서울대공원 신규 approved seed 60건을 승인했고, 총 approved/public/active POI는 305건이다.
- 2026-06-17 수도권 3차와 전국 주요 도시 1차 seed batch를 admin import/review workflow로 반영했다. 수도권 3차 batch `ee62c190-595e-4e42-a50c-56301b8dcfb2` 80건과 전국 주요 도시 1차 batch `88910305-736c-4e9c-bb09-3d0d735e4be9` 80건을 승인했고, 총 approved/public/active POI는 465건이다.
- 2026-06-20 Kakao Local hard delete는 보류로 판정했다. detail tap Android 증적은 고양/하남·미사/광주천 3개 Ready 권역에서 확보했고, 총 approved/public/active POI는 685건으로 유지한다.
- 2026-06-21 전국 seed 3차/4차 확대를 완료했고 총 approved/public/active POI는 985건이다. 다음 액션은 전국 seed 5차 확대 또는 Kakao Local shared provider 분리 설계다.
- 2026-06-21 전국 seed 5차 확대와 대량 seed 품질 점검을 완료했고 총 approved/public/active POI는 1,145건이다. 다음 액션은 Kakao Local shared provider 분리 closeout 또는 광역시/전국 seed 운영 품질 점검이다.
- 2026-06-22 Kakao Local shared provider 분리 closeout을 완료했다. 산책/location discovery는 shared provider 직접 참조를 제거하고 `walkKakaoFallbackProvider` adapter를 사용한다. pet-friendly adapter를 분리했고, 동물병원 provider matching과 coord2region Edge Function은 영향 없이 유지한다. 1,145건 POI 품질 재점검과 Android 통영 리스트/상세/empty UX smoke를 통과했다. 이후 walk-domain Kakao fallback은 제거됐고 admin queue/batch 확인성은 구현 완료됐지만, 운영자 QA closeout은 앱 출시 blocker가 아니므로 Parking으로 이동한다.

### 닫힘. 지도/API 비용 방어 hard cap 0 Android smoke

- 2026-05-27 repo와 linked remote 기준 Google Places/Place Photos provider runtime은 hard cap 0 safe no-op으로 닫혔다.
- `place-enrichment-demand` v12, `place-enrichment-worker` v9, `location-discovery-seed` v10 배포를 확인했다.
- production secret `NURI_PLACE_ENRICHMENT_HARD_CAP=0`을 설정했고, `GOOGLE_PLACES_API_KEY`는 제거했다. `GOOGLE_MAPS_API_KEY`는 secret 목록에 없다.
- Android 실기기 `R5CY613NMSY` / `SM_S937N`, 로그인 사용자 `test님` 세션에서 동물병원 리스트/상세/전화/길찾기, 산책 리스트/상세가 crash 없이 동작했다.
- logcat 기준 fatal crash 0건, unhandled promise rejection 0건, Google Places/Place Photos runtime 호출 0건, direct Google Places/Directions/Geocode API URL 0건이다.

## v1.0 closeout gate 상태

- 이 항목들은 신규 기능 개발이 아니며, 2026-06-04 기준 P0/P1 closeout은 닫힌 상태로 유지한다.
- Naver OAuth V1.0 public surface soft disable 확인
- 운영자 QA / 실기기 최종 스모크
  - 2026-06-02 기준 닫힘. UI 버튼 직접 탭 3회 증적은 P2 evidence gap으로 유지.
- 앱 스토어 출시 자산 셋업
  - V1.0 기능/QA blocker가 아니며 V1.1 작업도 아니다. 전체 개발/QA 종료 후 최종 제출 직전 준비 단계에서 진행한다.
- 최종 제출용 RC 빌드 확정
  - release APK 생성과 package/version/signing provenance는 고정 완료.
  - exact release APK 설치 smoke는 2026-06-02 기준 닫힘.
- final RC evidence baseline 고정
- 최종 제출용 clean RC build artifact/provenance 고정
  - release artifact/provenance는 고정 완료, exact install smoke만 남음.
- release risk ledger 전수 정리와 남은 P0/P1/P2 재분류는 `docs/qa/v1.0-remaining-task-risk-ledger.md`로 닫았다.

## v1.0 Auth / Social Login Scope

- Google/Kakao social login은 v1.0 public provider로 고정한다. Naver는 app-side 구현을 유지하되 V1.0 public surface에서는 soft disable한다.
- Google/Kakao는 Supabase built-in provider, Naver는 Supabase custom OAuth provider id `custom:naver`를 사용한다.
- 2026-05-11 Supabase Dashboard 기준 Google/Kakao는 disabled, Naver `custom:naver`는 Enabled다.
- Google OAuth는 기존 테스트 Google 계정과 `My First Project`를 사용하지 않는다. `My First Project`는 Places API 비용 리스크 프로젝트로 격리하며, NURI OAuth는 새 NURI Google 계정의 OAuth-only project 기준으로 진행한다.
- provider credential 발급/입력과 readiness true 전환은 Google/Kakao만 V1.0 사용 범위로 반영한다. Google/Kakao OAuth success smoke는 Codex Android evidence로 닫혔다. Naver 성공 session smoke는 더 이상 V1.0 blocker가 아니며 V1.1 또는 출시 후 재검토한다.
- provider console setup guide와 로그인 보안/API 방어 기준은 `docs/auth/social-provider-console-setup-guide.md`로 고정했다.
- provider activation-ready flag 기본값은 Google/Kakao `true`, Naver `false`다. Naver는 env가 잘못 켜져도 V1.0 public-surface guard에서 닫힌다.
- social login 버튼 하단에는 이용약관/개인정보처리방침 확인 및 동의 간주 고지 UI를 둔다.
- v1.0에서는 별도 social consent DB snapshot, 신규 migration, 신규 RPC를 열지 않는다.
- Apple social login은 v1.0 제외다.

## v1.0에서 열지 않는 것

- 실제 결제/과금 구조를 열지 않는다.
- Premium AI reply를 열지 않는다.
- Guestbook private letters 확장 아키텍처를 열지 않는다.
- Guestbook은 기존 단순 방명록 상태로 출시한다.
- Typography foundation rollout을 진행하지 않는다.

## 이미 닫힌 항목

- 정책 문서 public 연결과 앱 복귀
- 비밀번호 재설정 복귀
- 계정 탈퇴 7일 유예와 자동 파기 worker 배포
- 커뮤니티 신고 정책, auto-hide, reporter flag trace, image cleanup pending 계약
- 닉네임 정책 2..10 정렬과 Android 레이아웃 보정
- 커뮤니티 인앱 정책 notice
- 건강관리 리포트 Phase 1 repo 구현, linked remote 반영, row-level 검증, Android 실기기 핵심 QA
- ESLint release gate error 5건 fix-only 정리
- 건강관리 전용 기록하기 1차 전환
- 타임라인 `건강` 신규 작성 선택지 제거와 기존 health 기록 read-path 유지
- 생활/일정 병원·약 신규 작성 진입점 이관과 일정추가 UI 보더/아이콘 보정
- 건강관리 인사이트 탭 일반 사용자용 월간 요약/그래프 전환
- 동물병원 유저 서비스 기능 기준 close. 운영자 검수 UI approve/reject/held 실계정 QA는 전역 operator final gate로 분리한다.
- 날씨 도메인 비용 방어: `weather-cache` remote deploy, `nuri_weather_cache` remote apply, Android 실기기 홈/상세 QA, Open-Meteo client direct call 0건 검증
- 지도/API 비용 방어 V1.0 repo contract: `NURI_PLACE_ENRICHMENT_HARD_CAP` 기본값 0, Google Places/Place Photos runtime safe no-op, 산책/location discovery seed 반복 호출 캐시, release gate 문서화
- 지도/API 비용 방어 Android smoke: hard cap 0 / Google Places key 제거 상태에서 동물병원 리스트/상세/전화/길찾기, 산책 리스트/상세가 Android 로그인 사용자 세션에서 crash 없이 동작했다.
- social login v1.0 app-side 구현: Kakao/Google/Naver 버튼은 Supabase OAuth web flow에 연결했고, callback은 `nuri://auth/callback`으로 password reset `nuri://auth/reset`과 분리했다.
- social login provider gate: Google/Kakao는 V1.0 public provider로 닫혔고, Naver는 V1.0 public surface에서 soft disable한다. Supabase `custom:naver` provider와 app-side 코드는 hard delete하지 않는다. 실제 secret 값은 repo에 기록하지 않는다. Apple은 v1.0 제외다.
- Release Evidence Pack hard-close: Android RC smoke 조건부 통과, linked remote migration/lint/functions 확인, P0 없음, P1은 clean RC artifact 기준 evidence 고정 1건으로 제한
- Supabase DB Migration Dry-run / 원격 Apply: local/remote migration은 `20260429130000`까지 일치했고 remote apply는 no-op으로 닫았다. `db diff --linked` command는 Supabase CLI temp role/Docker 선행조건으로 `blocked` evidence를 남겼지만, pending migration과 destructive apply risk는 없다.
- 앱 아이콘 최종본 확정
- 스플래시 화면 최종본 확정

## v1.1 신규 업데이트 후보

- 전체 브리핑 source of truth: `docs/project-memory/v1.1-roadmap-briefing-2026-06-04.md`
- 추천 1순위: V1.1 추가 업데이트 정책 확정. 데일리 streak와 XP/칭호는 기준이 정해지기 전까지 구현하지 않는다.
- 추천 2순위: V1.1 추가 업데이트 2차 MVP 범위 확정. streak/deaily board와 알림 read path는 데이터/RPC 계약을 먼저 확정한 뒤 구현한다.
- Parking: admin UI 운영자 QA closeout. 앱 안 운영자 도메인은 출시 전 사용자-facing blocker가 아니며, 별도 홈페이지/관리 페이지 트랙에서 재개한다.
- 2026-06-30 기준 release candidate 기능/뒤로가기 audit:
  - 신규 QA 계정 온보딩과 로그아웃/재로그인 home 복귀 완료
  - 같은 사용자 email/password 재로그인 stale onboarding blocker 최소 수정 완료
  - 주요 사용자-facing 화면 17개 상단 뒤로가기/Android back audit 통과
  - Animal Hospital 전국 확장 완료, coordinate missing 122건은 release blocker 아님
  - Walk/POI approved/public/active 1,145건과 Kakao 차단/safe UX 유지

- Entitlement / billing foundation
  - `user_subscriptions`, `user_entitlements`, billing event contract는 v1.1 신규 업데이트 후보로 관리한다.
- Premium AI reply
  - AI notice, consent, generation log, provider policy는 v1.1에서 별도 설계한다.
- Guestbook private letters 확장 아키텍처
  - 상세/수정/삭제, AI reply 연동, 확장 IA는 v1.1 후보로 둔다.
- Typography foundation rollout
  - AppText/token 전면 적용은 v1.1 시각 정비 트랙으로 둔다.
- Apple social login
  - Android-first v1.0에서는 제외하고 iOS 출시 및 Apple 정책 검토 시점에 별도 판단한다.
- 장소/동물병원 `confirmed` 개방 검토
- 동물병원 admin 계정 조작 증적 고도화
- 커뮤니티 preview batch 최적화
- 운영자용 moderation/admin UI
- auth/account baseline-history 정리
- 외부 API 키 통제, 환경 분리, 운영 도구 정리
- 건강관리 후속 정리
- premium 인사이트 연결
- Open-Meteo customer API key/계약 확인
- `weather-cache` public no-JWT endpoint abuse throttle/rate limit
- MapLibre React Native 검토
- PMTiles/OSM 기반 자체 타일 호스팅 검토
- 산책/location discovery 자체 POI DB 구축
  - 산책/공원/산책로/반려동물 동반 가능 지점 seed를 공공데이터, OSM, 운영자 승인 seed, admin batch import 기반으로 관리한다.
  - Supabase PostGIS 기반 bbox/radius/distance query와 trigram/full-text 검색을 적용한다.
  - Kakao Local은 사용자 runtime에서 제거하고 필요 시 admin seed 보조 도구로만 제한한다.
- provider admin batch import 도구화
- place enrichment stale cache refresh 고도화는 provider admin/batch 경로에서만 검토

## 왜 이 순서인가

- v1.0은 더 이상 신규 기능을 늘리지 않는 Code Freeze 기준선으로 닫혔다.
- Supabase DB migration apply gate는 pending migration 0개/no-op apply로 닫혔다.
- Google/Kakao social login은 Android success smoke까지 닫혔고, Naver는 V1.0 public surface에서 soft disable한다. OAuth는 더 이상 V1.0 P1 잔여가 아니다.
- 과금, Premium AI, private letters 확장, typography rollout은 v1.0 완성도를 흔드는 신규 기능이므로 v1.1 업데이트 후보로 분리한다.
- MapLibre/PMTiles/자체 POI/PostGIS는 출시 직전 지도 비용 방어가 아니라 V1.1 구조 고도화이므로 V1.0에서는 provider runtime 차단만 닫는다.
- release APK artifact/provenance와 exact release install smoke는 고정됐다. Play Store 제출 자산은 V1.1 착수 전 gate가 아니라 전체 개발/QA 종료 후 최종 제출 직전 준비로 분리한다.
- 무한루프 방지를 위해 이미 close된 도메인 재조사, app-side social login 재구현, clean artifact 전 RC smoke 반복은 금지한다.
