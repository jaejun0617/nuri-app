# 현재 프로젝트 상태

## 프로젝트 현재 목적

NURI는 반려동물 기록, 일정, 탐색, 정보 소비, 공개 커뮤니티를 통합한 생활형 모바일 앱이다.

현재 5차의 목적은 기능 확장이 아니라, 이미 열려 있는 기능 축을 배포 가능한 서비스 계약과 운영 방어선 기준으로 다시 잠그는 것이다.

이번 단계의 최우선 기준은 아래 4가지다.

- 회원가입과 계정 화면을 실제 정책 문서와 연결한다.
- 인증을 가입 가능 상태에서 복구 가능 상태로 올릴 준비를 한다.
- 공개 도메인보다 운영 방어선을 먼저 닫는다.
- Candidate / Trust / User Layer 경계를 문서와 코드에서 일치시키는 방향을 고정한다.

Task 4 종료 정리 + Task 5 1차 최소 구현 + Task 5-5 User Layer 연결 + Task 5-6 stale/conflict 운영 기준 보강 기준 현재 상태는 아래처럼 갱신됐다.

- 정책 문서 source of truth는 `src/services/legal/documents.ts`로 모였다.
- `docs/policies/` 아래에 이용약관/개인정보처리방침/계정 삭제 안내 초안 구조가 추가됐고, 현재 상태는 `앱 연결용 초안`으로 고정됐다.
- 회원가입 화면은 필수/선택 동의 UI, 문서 상태, 초안 요약, consent snapshot 버전 참조가 같은 source of truth를 보도록 맞췄다.
- 회원가입 동의는 이제 하나의 카드 섹션 안에서 accordion 행과 `모두 동의하기` 편의 토글로 정리됐다.
- More 화면은 삭제 실행 로직을 유지한 채, 삭제 요청/정리 완료/안내 문서 미정 상태를 보수적으로 드러내도록 정리했다.
- Task 2 구현으로 `delete_my_account()`는 더 이상 단순 void RPC가 아니라, 삭제 요청 상태와 cleanup queue를 남기는 JSON 응답 계약으로 바뀌었다.
- `account_deletion_requests`, `account_deletion_cleanup_items`가 추가돼 요청/진행 중/cleanup pending/완료/실패/unknown 상태를 추적할 수 있게 됐다.
- `user_consent_history`는 hard delete 대신 `user_id null + anonymized_at + anonymized_subject_id` 기준 비식별 보관으로 전환됐다.
- `reports`, `pet_place_user_reports`, `pet_travel_user_reports`, `pet_care_guide_events`, `audit_logs`, `pet_place_search_logs`, `pet_place_verification_logs`는 user 식별자를 제거하는 익명화 보관 기준이 SQL 계약에 반영됐다.
- storage(`pet-profiles / memory-images / community-images`)는 삭제 RPC 안에서 동기 완료하지 않고, cleanup item queue를 남긴 뒤 `completed_with_cleanup_pending` 또는 `cleanup_pending` 상태로 분리 추적한다.
- More 화면 계정 삭제 메시지는 “즉시 완전 삭제” 대신 요청 처리/cleanup pending/unknown 확인 중 상태를 보수적으로 안내하도록 맞췄다.
- Task 2 적용 후 최종 검증에서 SQL과 앱 계약의 큰 불일치는 발견되지 않았고, 유효 상태값 일부가 앱에서 조용히 빠질 수 있던 분기와 QA gate의 자동/수동 검증 구분만 최소 보정했다.
- Task 3 구현으로 비밀번호 재설정은 `Supabase 기본 password reset email link + custom scheme(nuri://auth/reset) 복귀 + 앱 내 새 비밀번호 설정 화면` 조합으로 연결됐다.
- `App.tsx`에는 React Navigation `linking` 객체가 추가됐고, Supabase recovery redirect의 `#fragment` 토큰을 query 형태로 정규화해 `PasswordResetRecovery` route가 받도록 정리됐다.
- Android `AndroidManifest.xml`, iOS `Info.plist`, `AppDelegate.swift`에 custom scheme 수신 경로가 추가됐다.
- SignIn의 `비밀번호 찾기`는 더 이상 placeholder가 아니고 `PasswordResetRequest`로 진입한다.
- reset 완료 후에는 recovery 세션을 로컬에서 정리하고 SignIn으로 reset 이동한 뒤 `비밀번호가 변경되었습니다. 새 비밀번호로 다시 로그인해 주세요.` 안내를 띄우도록 구현됐다.

## 현재 아키텍처 요약

- 앱: React Native + TypeScript
- 상태/조회: Zustand, TanStack Query
- 백엔드: Supabase Auth / Postgres / Storage
- 위치 기반 공통 레이어
  - `useCurrentLocation`
  - `useDistrict`
  - `services/location/*`
  - `services/locationDiscovery/*`

## 완료된 큰 단계

- 기록/이미지 구조 1차 안정화
- 집사 꿀팁 가이드 앱/운영 구조 1차 완료
- 산책 기능 단순화
  - 현재 GPS 기반 주변 추천 + 직접 검색
  - 현재 위치 기준 거리
  - 상세 연관 장소 더보기
  - 키보드/스크롤 안정성 보강

## 진행 중 단계

- 5차 실행 체계 문서화 완료
  - `docs/프로젝트-현황-최종기획/5차-최종-기획서.md` 추가
  - Task 4는 repo 기준 구현 완료, remote blocker 해소, linked project 재검증까지 반영돼 공식 종료 상태로 잠겼다.
  - Task 5는 1차 최소 구현, User Layer 정합성 연결, stale/conflict 운영 기준 보강, Android emulator 기준 최소 UI 보정, 그리고 이번 턴 `후보 노출 억제 read-path 보수화`까지 반영됐다.
  - 이번 턴 read-path 보수화로 place explicit query, walk nearby default, travel region-only/broad query에서 trust 없는 후보 총량과 상단 점유율을 더 보수적으로 줄였다.
  - 다만 물리 실기기 연결과 Task 4 row-level 운영 캡처는 외부 blocker registry로만 유지하고, 이번 구현의 선행조건으로 잡지는 않았다.
  - Task 5 physical-device QA는 release blocker 유지 상태로만 둔다.
  - linked project `grmekesqoydylqmyvfke` 기준 profile nickname path의 실제 운영 source of truth는 `blocked_nicknames / blocked_nickname_patterns / normalize_nickname_for_policy() / enforce_profile_nickname_policy() / trg_profiles_nickname_policy` 조합으로 다시 확인됐다.
  - repo mirror는 `docs/sql/인증-계정/프로필-닉네임정책-현재로컬미러.sql` 기준으로 remote shape를 다시 맞췄다.
  - 이번 턴 Task 13 판정 기준 닉네임 정책 execution lineage 공백은 단독 예외가 아니라, 현재 `supabase/migrations/*`가 recent delta 위주이고 pre-Task6 auth/account baseline을 담지 않는 구조 안에 있다.
  - 따라서 닉네임 정책만 별도 idempotent migration으로 뒤늦게 추가하는 방식은 지금 시점의 권장안이 아니다. 현재는 baseline/history 재구성 설계 항목으로 남기고, runtime source of truth와 execution lineage를 분리해 유지한다.
  - 이번 턴 Task 14 설계 잠금 기준 pre-Task6 auth/account baseline-history 권장 구조는 단일 대형 migration이나 docs-only 유지가 아니라, `auth core(profiles + own RLS + on_auth_user_created) / account deletion + consent / nickname policy` 3축 executable backfill 패키지다.
  - baseline-history 포함 대상은 `profiles`의 auth/account 소유 계약, `handle_new_user()`, `user_consent_history`, `account_deletion_requests`, `account_deletion_cleanup_items`, 계정 삭제 helper/RPC/grant/trigger, `blocked_nicknames`, `blocked_nickname_patterns`, `normalize_nickname_for_policy()`, `enforce_profile_nickname_policy()`, `check_nickname_availability()`, 관련 trigger/policy/grant로 잠근다.
  - 반대로 blocked nickname 운영 row seed, bootstrap 전체판, `profiles.role` 같은 타 도메인 확장, 공용 helper의 기원 설명은 이번 baseline-history executable에 섞지 않고 문서/별도 선행 의존성으로 남긴다.
  - 다음 직접 구현 턴 이름은 `Task 14-1 pre-Task6 auth/account baseline-history executable backfill 구현`으로 잠갔고, 이번 턴 Task 14-2에서 fresh remote 재검증 뒤 실제 executable SQL draft까지 채웠다.
  - `supabase db dump --linked --dry-run`, `supabase migration list --linked`, `supabase db query --linked` 기준 fresh remote 재검증 결과, linked remote에는 여전히 `profiles.role`, legacy `delete_my_account()` void overload, broad routine execute ACL, nickname trigger/RPC와 분리된 legacy `profiles_nickname_length_check(2..24)`가 남아 있음을 다시 확인했다.
  - 그 결과 skeleton 3개는 여전히 `auth core / account deletion + consent / nickname policy` 3축 구조를 유지하되, `auth core`는 excluded dependency(`profiles.role`, `set_updated_at()`)를 건드리지 않는 guarded baseline, `account deletion + consent`는 docs-final JSON 계약 본문 + legacy zero-arg overload drop draft, `nickname policy`는 fresh remote verified table/function/trigger shape를 주석과 함께 고정한 executable body로 채워졌다.
  - 다만 fresh remote 기준 `profiles` table constraint와 nickname trigger/RPC strictness는 완전히 같은 계약이 아니다. 현재 runtime strictness는 `enforce_profile_nickname_policy()`, `check_nickname_availability()`, safe `handle_new_user()` 쪽이 더 강하고, table-level `profiles_nickname_length_check`는 여전히 bootstrap legacy 2..24 shape다.
  - 이번 턴 Task 14-3 review 결과, remote apply 전에 반드시 고칠 항목은 둘로 잠겼다. `auth core`는 linked remote가 이미 더 안전한 `handle_new_user()` body를 쓰고 있으므로 baseline body 재바인딩을 제거하고 현 runtime을 preserve해야 하며, `account deletion + consent`는 legacy `delete_my_account()` zero-arg overload drop을 유지한 채 revoke/grant를 `public`뿐 아니라 `anon/authenticated/service_role` direct grant까지 다시 좁히는 deterministic grant block으로 보강해야 한다.
  - 반대로 nickname axis의 table-level `profiles_nickname_length_check`는 이번 baseline-history remote apply에서 2..24 legacy shape를 그대로 둔다. 현재 운영 strictness는 `enforce_profile_nickname_policy()`, `check_nickname_availability()`, safe `handle_new_user()` 쪽이 더 강하고, 지금 table constraint까지 2..8로 조이면 기존 row/data audit 없이 운영 충돌을 만들 수 있기 때문이다.
  - 이번 턴 Task 14-4에서 linked remote live catalog 재조회가 실제로 성공했고, `migration history 5개`, `profiles_nickname_length_check(2..24)`, final safe-generator `handle_new_user() + on_auth_user_created`, legacy `delete_my_account()` void overload 실존, account deletion routine broad execute ACL, nickname policy function/trigger/policy shape를 다시 확인했다.
  - 그 결과 최종 승인 판정은 그대로 유지됐다. `auth core`의 preserve/no-op는 live remote signup runtime을 downgrade하지 않고, `account deletion + consent`의 overload drop + deterministic grant narrowing은 live remote broad ACL/legacy overload를 정확히 수축시키며, `nickname policy`의 table-level constraint는 여전히 intentional preserve가 맞다.
  - 이번 턴 Task 14-5에서 linked remote `supabase db push --linked`를 실제 실행해 `20260330140442/43/44` 3개 backfill migration을 모두 반영했다.
  - apply 중 `20260330140444_backfilled_baseline_history_nickname_policy.sql`은 remote `public.profiles.nickname`이 이미 `citext`이고 `trg_profiles_nickname_policy`가 해당 컬럼에 의존하는 상태에서 무조건 `alter column ... type citext`를 다시 시도해 실패했다. 원인 범위 최소 수정으로 `nickname` 타입이 이미 `citext`면 해당 DDL을 건너뛰는 guard만 추가했고, 그 뒤 44번 migration 재적용이 정상 완료됐다.
  - apply 직후 live remote 재검증 결과 migration history는 `20260330140442/43/44`까지 8개로 확장됐고, `public.handle_new_user()`와 `auth.users`의 `on_auth_user_created`는 final safe signup path를 유지했다.
  - `delete_my_account()` zero-arg overload는 제거됐고, `delete_my_account(uuid)` 및 account deletion routine execute privilege는 `authenticated` 또는 `service_role` 필요 role만 남는 deterministic narrowing 상태로 수축됐다.
  - `profiles_nickname_length_check`는 의도대로 `2..24` table-level shape를 그대로 유지했고, `blocked_nicknames`, `blocked_nickname_patterns`, `normalize_nickname_for_policy()`, `enforce_profile_nickname_policy()`, `check_nickname_availability()`, `trg_profiles_nickname_policy`, 관련 select policy와 grant shape도 유지됐다.
  - 따라서 현재 상태는 `3축 backfill remote apply 완료`, `즉시 blocker 없음`, `별도 repair task는 nickname table-level constraint 2..8 alignment/data audit만 남음` 단계로 본다.
  - 이번 턴 구현으로 linked project remote와 repo mirror 양쪽에 `community_blocked_terms`, `community_blocked_term_patterns`, `assert_community_content_policy()`, `raise_community_write_error()`, `guard_community_post_update()`, `guard_community_comment_update()`가 추가됐다.
  - 커뮤니티 write-path는 이제 post create/update, comment create/update 모두 같은 서버 정책 축에서 검사되고, 앱은 `community_content_policy_violation` 등 stable error code를 기준으로 create/edit/comment UX를 공통 매핑한다.
  - comment edit 앱 경로는 여전히 없지만, 서버 update guard는 먼저 닫아 우회 저장 가능성을 줄였다.
  - create/edit/comment 실패 시 draft/input 유지 계약은 그대로 유지된다.
  - 이번 턴 운영 마감으로 linked project remote `community_blocked_terms` 4건, `community_blocked_term_patterns` 1건의 초기 seed가 실제 입력됐고, post/comment create/update 차단이 remote 기준으로 다시 검증됐다.
  - false positive 최소 검증에서는 제목 `시발점`, 본문 `강아지 새끼`, 댓글 수정 문장 `ㅆ 와 ㅂ 를 따로 적은 문장`이 모두 통과해 초기 seed가 과도하게 넓지 않음을 확인했다.
  - Android emulator 기준 수동 UX 검증에서 post create, post edit, comment create 차단 토스트는 모두 raw 서버 문구가 아니라 보수적인 한국어 문구로 노출됐고, 입력값도 같은 화면에 그대로 남아 즉시 수정 재시도가 가능함을 실제 화면 dump로 다시 확인했다.
  - comment edit 앱 경로는 `src/services`, `src/store`, `src/screens/Community` 기준으로 이번 턴에도 없음을 재확인했고, 앱 UX 검증 대상은 create/edit/comment create까지만 유지했다.
  - 다만 create/edit 이미지 upload warning 또는 image-pick 오류 노출은 이번 턴에 수동 재현하지 못해, branded error code path 연결 상태로만 남아 있다.
  - local repo에는 같은 내용을 담은 idempotent seed migration이 있지만, linked temp-role push 경로는 circuit breaker에 막혀 이번 턴 remote migration history sync까지는 재확인하지 못했다.
  - 이번 턴 최소 구현으로 커뮤니티 리스트는 더 이상 `latest comment preview` 때문에 post별 전체 comments 배열을 eager fetch하지 않는다.
  - 리스트 read-path는 `fetchLatestCommunityCommentPreview(postId)` 단건 fetch와 `latestCommentByPostId` preview cache 기준으로 좁혔고, 상세용 `fetchPostComments(postId)` 전체 댓글 계약은 그대로 유지했다.
  - 리스트 프리뷰 경로에서는 `comment_likes` 조회와 thread regroup를 더 이상 돌리지 않는다.
  - Task 7 후속 판정 기준 현재 남은 리스트 성능 리스크는 `commentCount > 0`인 post마다 preview fetch가 1회씩 도는 round-trip 수다.
  - 다만 이 잔여 비용은 selector/store 병목보다 fetch granularity 문제이고, linked project remote에도 preview batch 전용 함수/뷰가 없어 의미 있는 2차 개선은 서버 read contract를 추가해야 한다.
  - 따라서 Task 7은 여기서 닫고, Task 7-2는 실제 체감 병목 증적이 생길 때만 별도 성능 슬라이스로 연다.
  - 이번 턴 구현으로 linked project remote `public.posts.view_count`, `public.community_post_view_events`, `record_community_post_view()`가 실제 배포됐다.
  - 조회수는 더 이상 placeholder만 있는 상태가 아니다. list/detail SELECT는 `view_count`를 실제로 읽고, `CommunityDetailScreen -> recordPostView(postId)` detail-open write 경로가 server-confirmed count만 캐시에 반영한다.
  - dedupe 기준은 `로그인 user_id` 또는 `guest_session_id`이고, self-view는 집계하지 않는다. 같은 사용자 또는 같은 guest session의 6시간 내 재진입은 서버에서 무증가로 처리한다.
  - guest session source of truth는 앱 로컬 `nuri.community.guest-session.v1`이고, 로그아웃/탈퇴로 지우지 않아 anonymous 재진입 dedupe를 유지한다.
  - local/remote drift는 해소됐다. 다만 Task 5 physical-device QA는 release blocker 유지 상태로만 둔다.
  - 이번 턴 Supabase SQL 정리 기준으로 `supabase/migrations/*`는 linked remote와 `db push --dry-run` 기준 동기화된 실행 이력으로 분류했다.
  - `docs/sql/`은 한글 폴더명 기준의 사람이 읽는 최종본 체계로 재정리했고, `docs/sql/<도메인명>/*-최종.sql`은 문서용 최종본 source of truth, `docs/sql/<도메인명>/레거시/*`는 단계별 구버전 SQL로 분리했다.
  - 새 빈 DB 재구축 기준 적용 순서는 `docs/sql/공용/새빈DB-재구축-적용순서.md`와 `scripts/rebuild_public_schema_from_docs_sql.sh`로 잠갔다.
  - 이 재구축 세트는 새 빈 DB 또는 public/storage 재구축 직후 상태 전용이며, 기존 운영 프로젝트 위에 `누리-전체초기구성-부트스트랩.sql`을 그대로 다시 덮는 용도로 쓰지 않는다.
  - linked remote runtime 확인 기준 `memory_images`, `pet_place_service_meta`, `pet_care_guides`, `user_consent_history`, `account_deletion_requests`, `community_post_view_events`는 실제 존재한다.
  - 이번 턴에 `docs/sql/반려동물과-여행/반려동물과여행-트러스트레이어-최종.sql`을 execution mirror `supabase/migrations/20260329192934_task9_pet_travel_trust_layer.sql`로 반영했고, linked remote에 `pet_travel_places`, `pet_travel_pet_policies`, `pet_travel_user_reports`, `is_pet_travel_admin()`가 실제 생성됐다.
  - 프로필 닉네임 정책의 repo/remote mirror drift는 `docs/sql/인증-계정/프로필-닉네임정책-현재로컬미러.sql` 갱신으로 정리됐다. 다만 `supabase/migrations/*`에는 이 경로의 실행 migration 이력이 아직 없다.
  - 현재 `supabase/migrations/*`는 최근 커뮤니티 Task 6/8 delta 위주 실행 이력이라 프로젝트 전체 baseline 세트가 아니며, 설명 문서 3개만 안전 삭제했고 SQL 실행 파일은 삭제하지 않았다. migration baseline 재구성도 full remote dump와 빈 DB 재구축 검증 전까지는 보류한다.
- Task 1 완료
  - 회원가입 동의 UI와 정책 문서 source of truth 연결 구조 정리
  - 정책 문서 미정 상태 fallback 추가
  - More 화면 삭제 안내 문구와 상태 확인 경로 정리
- Task 2.5 완료
  - 정책 문서 초안 구조를 `docs/policies/`에 추가
  - 회원가입 약관/개인정보/마케팅 동의를 하나의 카드 섹션과 chevron 기반 disclosure 구조로 개편
  - `모두 동의하기` 편의 토글을 추가하되 필수/선택 제출 계약은 유지
- Task 2 구현 완료
  - 삭제 상태 추적 테이블과 cleanup queue 계약 추가
  - hard delete / 익명화 / 비식별 보관 정책을 SQL에 반영
  - More 화면 최소 연동과 보수적 상태 메시지 반영
  - release checklist를 계정 삭제 QA gate 기준으로 강화
- Task 3 구현 완료
  - `PasswordResetRequest`, `PasswordResetRecovery`, `PasswordResetForm` 3개 화면으로 reset 흐름을 최소 구성했다.
  - reset 요청은 enumeration 방지 문구와 60초 재발송 쿨다운을 적용했다.
  - recovery 링크는 `nuri://auth/reset` custom scheme으로 앱에 복귀하고, fragment token을 navigation params로 변환해 recovery 세션을 연다.
  - 만료/오류 링크는 SignIn이 아니라 `PasswordResetRequest`로 복귀시키고 즉시 재요청 가능 흐름을 유지한다.
  - reset 성공 후 자동 로그인은 하지 않고 recovery/local 세션을 정리한 뒤 SignIn 재로그인 유도로 마무리한다.
- Task 4 종료 정리 완료
  - `docs/sql/커뮤니티/커뮤니티-운영-최종.sql` 기준으로 게시글/댓글/신고/이미지 업로드의 서버측 guard trigger와 moderation 보조 테이블 계약을 추가했다.
  - 게시글은 `5분 3회 + 1시간 10회`, 댓글은 `1분 5회 + 10분 20회`, 신고는 `10분 5회 + 1일 20회` 기준으로 DB trigger가 직접 차단한다.
  - 게시글 `제목+본문`, 댓글 `동일 post 내 동일 내용`은 30초 short-window dedupe를 서버 기준으로 막는다.
  - `community_moderation_queue`, `community_moderation_actions`, `community_reporter_flags`, `community_image_assets`를 추가해 신고 triage, auto-hide, action log, 반복 악성 신고 추적, orphan/cleanup 상태를 남기도록 정리했다.
  - auto-hide는 unique reporter 기준으로 게시글 3명, 댓글 2명 이상일 때 `auto_hidden`과 queue/action log를 함께 남기도록 구현했다.
  - `community-images`는 업로드 빈도 제한과 active post 기준 select 정책을 추가했고, hidden/auto_hidden/deleted/banned 상태에서는 signed URL을 다시 노출하지 않도록 앱과 storage 정책을 함께 보강했다.
  - repo 기준 운영 검증에서 다중 이미지 업로드 일부 실패 path를 cleanup queue로 회수하도록 보정했고, 신고 직후 auto-hidden 된 게시글이 detail 재조회 후 list 캐시에 active 상태로 남지 않게 정리했다.
  - Task 4 후속 버그 점검에서 댓글 작성자명이 `알 수 없는 사용자`로 바뀌던 원인은 `comments.user_id` 또는 `profiles` row 유실이 아니라, 댓글 author 조립이 `profiles` 조회에만 의존하고 `profiles_select_own` RLS 때문에 타인 프로필이 null로 내려오는 구조임을 확인했다.
  - 최소 보정으로 댓글 정규화 시 public active post의 `author_snapshot_nickname/avatar`를 보조 author source로 사용하고, 신고 후 refetch가 이미 알고 있던 comment author 메타데이터를 `알 수 없는 사용자`로 덮어쓰지 않도록 store merge를 보강했다.
  - release checklist는 repo 기준 확인 항목과 staging/prod 운영 증적 항목을 분리하는 방향으로 보강했다.
- Task 4 완료 후 운영 메모
  - `community_reporter_flags`의 rate-limit trace는 trigger 내부 영속화가 아니라 앱 차단 응답 직후 follow-up RPC 기준으로 남는다.
  - 전용 운영 UI는 아직 없고, `community_moderation_queue / community_moderation_actions / cleanup_pending` 확인은 SQL/콘솔 절차에 의존한다.
  - restore/manual cleanup SOP는 release 직전까지 보강 권장 사항으로 남는다.
  - linked project의 `qa-task4-*` 원격 테스트 데이터는 release 전 정리 체크포인트로 유지한다.
- Task 5 1차 최소 구현 완료
  - `src/services/trust/publicTrust.ts` 기준 public label을 `candidate / needs_verification / trust_reviewed` 3단계로 고정했고, UI에서는 `후보 / 확인 필요 / 검수 반영`만 노출한다.
  - `펫동반 장소 탐색`과 산책은 `LocationDiscoveryItem.publicTrust`를 중심으로 candidate / trust / user layer 설명과 공개 라벨을 분리해 노출한다.
  - `펫동반 장소 탐색`은 `placeMeta.ts`에서 `last_verified_at`까지 읽어 와 candidate normalize 이후 trust overlay를 read-only로 반영하고, 검색어가 없을 때 trust 근거 없는 후보 노출을 더 낮춘다.
  - `반려동물과 여행`은 `src/services/supabase/petTravelTrust.ts`를 통해 `pet_travel_places / pet_travel_pet_policies`를 read-only로 읽고, TourAPI candidate 결과를 정규화한 뒤 public label을 다시 계산한다.
  - 여행 리스트/상세는 `후보 / 확인 필요 / 검수 반영` 라벨, 짧은 근거 설명, 기준일, 재확인 권장 문구를 직접 노출하며 `confirmed` 문구는 계속 봉인한다.
  - trust read-only 연결 후에도 user layer는 public trust와 별도 저장소와 별도 UI 영역으로 유지하고, bookmark / recent search / user report가 public label을 직접 승격하지 않도록 유지한다.
- Task 5-5 User Layer 정합성 연결 완료
  - `pet_place_bookmarks`, `pet_place_user_reports`, `pet_travel_user_reports`를 current user own-scope read/write 경로로 연결했고, public trust 계산은 그대로 둔 채 별도 personal state로만 소비한다.
  - `pet-friendly-place`는 `pet_place_service_meta.id`가 있는 항목에서만 bookmark / user report를 열고, `반려동물과 여행`은 `pet_travel_places.id`가 연결된 항목에서만 own report를 기록한다.
  - 리스트/상세는 공개 라벨 영역과 `저장함 / 최근 검색 / 내가 제보함` 영역을 분리해 노출하고, 개인 상태가 공개 라벨을 올리지 않는다는 문구를 직접 유지한다.
  - recent search는 walk / pet-friendly-place / pet-travel별 로컬 저장소로 연결했고, place/travel 수동 검색은 authenticated 기준 `pet_place_search_logs`에 best-effort 기록을 남긴다.
  - local recent search는 로그아웃/계정 삭제 시 `clearLocalSessionState()`에서 함께 비워 Task 2 개인화 정리 계약과 충돌하지 않게 맞췄다.
- 펫동반 장소 탐색을 실제 서비스 구조로 개방
  - 현재 위치 주변 추천
  - 직접 검색
  - 리스트/상세/지도 연결
  - 출처/검증 상태 분리
- 커뮤니티 댓글 고도화
  - 상세 댓글 UI를 서비스형 스레드 구조로 재정리 중
  - 1-depth reply / comment_likes / best 댓글 기준 설계 확정
  - reply rail, 댓글 더보기 footer, 전송 버튼 활성 조건까지 1차 polish 반영
  - `Detail/List/EditorForm/PostCard`는 `.styles.ts` 분리까지 완료
  - 댓글 렌더는 `CommentThreadItem` / `ReplyCommentItem` / `CommentActionRow` 분리와 id/entity 기반 selector 구조까지 1차 반영
  - 다음 단계는 리스트 화면도 `postId list + item narrow selector + pure PostCard` 구조로 소비 범위를 줄이는 것이다
  - Android 실기기 키보드 바/IME 영역까지 포함한 댓글 composer 검증 기준 반영
  - 리스트는 `CommunityPostListItem` 도입과 `postIds` 기반 FlatList 전환까지 1차 적용됨
  - 커뮤니티 4개 화면은 `CommunityStackHeader` 기준 공통 stack header로 정리됨
  - 댓글/avatar와 리스트 card avatar는 `react-native-fast-image` 기준 캐싱 적용

## 현재 기준 핵심 도메인 상태

- 산책
  - 공통 `locationDiscovery` 레이어 기반
  - 서비스 추천 정렬 + 거리 정렬 제공
  - 위치 편의성과 공개 신뢰도는 같은 축으로 섞지 않고, 산책 후보는 `현재 위치 기반 후보` 성격의 보수 라벨로 노출한다.
- 펫동반 장소 탐색
  - 메뉴/화면 진입 존재
  - 현재 운영 데이터는 Kakao Local 외부 검색 결과
  - 자체 Supabase 장소 DB는 아직 없음
  - 리스트/상세/지도 미리보기/외부 지도 열기/연관 장소 더보기 구조 반영
  - `외부 후보`와 `서비스 검증 상태`를 분리한 내부 상태 모델 반영
  - `placeMeta.ts`에 `pet_place_source_links -> pet_place_service_meta -> pet_place_external_signals` 실제 조회가 연결됨
  - 현재 앱은 Kakao 후보를 normalize한 뒤, Supabase canonical 메타와 외부 보조 신호를 병합하는 구조까지 반영됨
  - 읽기 순서는 여전히 `candidate first + optional trust overlay`이며, canonical trust row만으로 리스트를 여는 trust-first 런타임은 아직 아니다.
  - 이번 턴에 `publicTrust` 계산과 `last_verified_at` read-only 연결이 추가돼 `후보 / 확인 필요 / 검수 반영`이 공개 라벨의 source of truth가 됐다.
  - 검색어가 없을 때 trust 근거와 키워드 근거가 모두 약한 후보는 더 낮은 정렬과 제한된 노출로 보수화했다.
  - 외부 소스 역할은 `Kakao 후보 수집 / Google Places 보조 속성 / 관광공사·공공데이터 보강 / Supabase 최종 메타`로 정리됨
  - 실제 서비스급 장소 메타 SQL 문서는 `docs/sql/펫동반-장소/펫동반장소-메타-최종.sql`
  - 신뢰도 보강 API 호출 전략 문서는 `docs/domains/펫동반-장소-탐색/펫동반-장소-탐색-신뢰도-보강-API-전략.md`
  - 장기 확장용 `반려동물과 여행` 분리 설계 문서가 시작됨
- 반려동물과 여행
  - MoreDrawer와 RootNavigator에 별도 메뉴/라우트가 열림
  - 현재 구조는 `검색 입력 -> 카테고리 선택 -> 리스트 -> 상세 -> 외부 지도 열기`
  - 현재 위치/GPS 기반 추천은 아직 넣지 않음
  - 핵심 후보 소스는 한국관광공사 반려동물 동반여행 서비스이며, 리스트/상세 조회가 TourAPI fetch 구조로 연결됨
  - 현재 런타임에는 `TOUR_API_SERVICE_KEY`가 들어가 있음
  - 2026-03-19 공식 Swagger 재확인 결과 유효 provider는 `KorPetTourService2`이며, 리스트/검색/상세 핵심 오퍼레이션은 `areaBasedList2`, `searchKeyword2`, `detailCommon2`, `detailIntro2`, `detailPetTour2`다.
  - 기존 live 실패 원인은 `areaBasedList`, `detailCommon`, `detailIntro` 같은 비공식 오퍼레이션 호출이었다.
  - 현재 mock 리스트 fallback은 없고, 공식 `KorPetTourService2/*2` 호출 기준으로 live 응답이 실제 확인된 상태다.
  - `searchKeyword2`는 지역명 단독 검색에서 쇼핑/브랜드 점포를 넓게 섞어 주거나 `강원도`처럼 0건을 돌려주는 경우가 있어, 지역명 단독 검색은 전국 공통 region resolver + `areaBasedList2` narrow mode로 처리한다.
  - region resolver는 특정 예시 지역 patch가 아니라 전국 시/도 + 주요 시/군/구/생활권 별칭 메타를 기준으로 동작한다.
  - 이번 턴 Task 10 기준 `petTravel.test.ts` 실패 1건은 confirmed 개방 문제가 아니라 region-only search/filter drift로 확정됐다. `제주도`처럼 resolver가 이미 지역 범위를 좁힌 검색에서도 점수 계산이 지역 scope 적합도를 반영하지 않아 유효 후보가 컷 아래로 떨어질 수 있었고, 현재는 region intent match에 한한 최소 travel score 가산점으로만 보정했다.
  - 이번 턴 Task 11 broad/specific intent 재판정에서는 live query 9건(region-only 3, broad 3, specific 3)을 실제 TourAPI로 다시 확인했다. broad/specific 구간은 대부분 `searchKeyword2` raw 0건이 먼저 발생해 app filter 과수정 여부를 입증할 증적이 부족했고, 현재는 broad/specific 로직을 추가 수정 없이 유지하는 편이 더 안전하다고 잠근다.
  - 검색 후처리는 `placeType`과 `petAllowed`를 분리하며, `travelScore / petScore / commercialPenalty`를 `0~1` 정규화 점수로 재구성했다.
  - TourAPI raw 문구만으로 `confirmed`는 만들지 않고, 현재 런타임에서는 `publicTrust` 기준 `후보 / 확인 필요 / 검수 반영`만 보수적으로 노출한다.
  - 장기 source of truth는 `장소 검색 API + TourAPI + 자체 DB` 조합이며, 앱 타입에는 `petPolicy`, `aggregation`, `places / pet_policies / user_reports` 확장 방향이 반영됐다.
  - 이번 턴에 Trust Layer DB 최종본이 linked remote까지 실제 적용돼 `pet_travel_places / pet_travel_pet_policies / pet_travel_user_reports` 기준 runtime drift를 해소했다.
  - 이번 SQL은 외부 원본 매핑 전용 `source_links`를 아직 만들지 않고, `places.primary_source + primary_source_place_id`로 bootstrap하는 방향이다.
  - 현재 앱 런타임은 TourAPI candidate를 기본 source로 삼되, `pet_travel_places / pet_travel_pet_policies` read-only snapshot을 함께 읽어 public label을 재결정한다.
  - trust data가 있어도 `검수 기준일 존재 + 90일 이내 + evidence 존재 + source conflict 없음 + onsite check 불필요`를 모두 만족할 때만 `검수 반영`을 유지하고, 하나라도 비면 `확인 필요`로 강등한다.
  - `pet_travel_user_reports`, place bookmark, recent search는 public trust와 분리된 실제 runtime personal state로 연결됐다.
  - 다만 canonical trust/place id가 없는 여행 후보와 service meta가 없는 장소 후보는 이번 턴에도 personal write path를 열지 않았다.
  - `confirmed` 타입은 남아 있지만 UI와 public label 분기에서는 계속 생성하지 않는다.
  - `펫동반 장소 탐색`은 `admin-verified`만으로 `검수 반영`을 열지 않고, `last_verified_at`, `pet_policy_text/admin_note/source link/external signal` evidence, 외부 신호 충돌 여부를 함께 본다.
  - 산책은 위치 편의성 중심 도메인이라 이번 턴에도 `candidate`만 유지하고, 공개 라벨을 올리지 않는다.
- 프로젝트 온보딩 문서
  - 현재 실행 기준의 최상위 총괄 문서는 `docs/프로젝트-현황-최종기획/5차-최종-기획서.md`다.
  - `docs/프로젝트-현황-최종기획/4차-최종-기획-진행서.md`는 과거 통합 점검 reference로 유지한다.
  - `docs/프로젝트-현황-최종기획/4차-우려사항-및-장기운영-리스크.md`는 과거 리스크 문서이지만, 항목별 현재 상태를 함께 적는 경고문으로 사용한다.
  - 새 세션은 engineering 규칙 문서 + 5차 기획서 + project-memory를 먼저 보고, 4차 문서는 교차 검증용으로 본다.
  - 다만 현재 작업 트리에는 커뮤니티 관련 미커밋 변경이 남아 있어, 공식 메모리 우선순위와 세션별 실작업 흐름이 어긋날 수 있다.
- 커뮤니티
  - 목록/상세/작성/수정/좋아요/댓글/신고/soft delete 구조는 이미 열려 있다.
  - 하단 탭 기준 진입점은 `CommunityTab`으로 승격됐고, 메인 탭 구조는 `Home / Timeline / Community / Guestbook / More` 기준이다.
  - 하단 탭의 `CommunityTab`도 별도 `CommunityTabStackNavigator`를 통해 공통 `CommunityStackHeader`를 받도록 정리됐다.
  - 현재 댓글은 `top-level 10개 + 더보기`, `reply preview 2개`, `reply는 depth 1 평탄화` 기준으로 고도화 중이다.
  - 댓글 좋아요는 `comment_likes` 별도 테이블과 optimistic update 기준으로 설계가 넘어갔다.
  - 상세 렌더는 `commentsByPostId` 배열 직접 렌더에서 벗어나 `commentEntitiesById`, `topLevelCommentIdsByPostId`, `replyCommentIdsByParentId` 기반으로 1차 최적화됐다.
  - 리스트 렌더는 `posts` 직접 renderItem 소비에서 벗어나 `postIds + CommunityPostListItem + pure PostCard` 구조로 1차 최적화됐다.
  - linked project remote 기준 `trg_guard_community_post_insert -> guard_community_post_insert()`와 `trg_guard_community_comment_insert -> guard_community_comment_insert()`는 INSERT 에만 걸려 있고 rate limit, dedupe, trim만 처리한다.
  - post/comment UPDATE 는 `posts_update_own`, `comments_update_own` RLS와 길이/status check, `set_updated_at()`만 있어 콘텐츠 금칙어 차단은 없다.
  - profile nickname path에는 remote 운영 source of truth `blocked_nicknames`, `blocked_nickname_patterns`, `enforce_profile_nickname_policy()`, `trg_profiles_nickname_policy`가 실제 존재하지만 커뮤니티 본문/댓글 write-path와는 분리돼 있다.
  - 조회수는 linked project remote `public.posts.view_count`와 `record_community_post_view()` 기준의 실제 운영 계약으로 전환됐다.
  - Task 7 최소 슬라이스 이후 리스트는 latest comment preview 때문에 post별 전체 comments를 읽지 않는다. preview는 `latestCommentByPostId` cache와 단건 read path로 좁혀졌다.
  - 현재 남은 커뮤니티 성능 리스크는 comment가 있는 post 수가 많은 페이지에서 preview fetch가 아직 post별 1회라 round-trip 수가 늘 수 있다는 점이다.
  - 조회수는 list impression이 아니라 detail open 기준으로만 증가하고, optimistic counter는 여전히 금지다.
  - store/entity 전면 재설계와 FlatList 엔진 교체는 이번 턴에도 열지 않았다.
- 기록
  - 기록 작성 화면은 더 이상 하단 탭 스크린이 아니고, `RootNavigator.RecordCreate` 독립 스크린으로 열린다.
  - 홈/타임라인의 기록하기 CTA는 `returnTo`를 유지한 채 root stack `RecordCreate`로 진입한다.
  - `RecordDetail`은 `entrySource` 기반 뒤로가기 복귀 규칙이 복구돼 홈/More 진입 흐름을 다시 보존한다.
  - 기록 상세/작성과 타임라인 리스트는 최근 톤 정리로 흰 배경과 보더 축소 기준을 사용한다.

## 주요 리스크

### 해결됨

- 비밀번호 재설정은 더 이상 미구현 리스크가 아니다. request/recovery/form, custom scheme 복귀, 재발송 쿨다운까지 코드에 반영됐다.
- 계정 삭제는 더 이상 단순 void RPC가 아니다. 요청 상태, cleanup queue, 익명화 보관, 보수적 사용자 메시지 계약이 반영됐다.
- 커뮤니티 abuse 최소 방어선은 repo 기준으로 구현됐다. rate limit, dedupe, auto-hide, moderation queue/action log, image cleanup_pending 계약이 SQL과 앱에 반영됐다.
- Task 4는 repo 기준 구현 완료, remote blocker 3개 해소, linked project `grmekesqoydylqmyvfke` 재검증까지 끝나 운영 기준으로 완료 판정 가능하다.
- 댓글 작성자 unknown 이슈는 DB 유실이 아니라 `profiles` 의존 + RLS 제약 문제로 확정했고, app fallback/store merge 보정까지 반영됐다.

### 부분 해결 / 운영 증적 필요

- 정책 문서 source of truth와 초안 구조는 생겼지만, 실제 약관/개인정보/삭제 안내의 최종 본문과 공식 링크는 아직 미정이다.
- 회원가입은 정책 상태를 숨기지 않지만, 사용자가 실제 전체 본문을 열람하는 최종 연결은 아직 없다.
- SQL 문서는 새 계약으로 갱신됐지만, staging/prod 실제 적용 여부는 아직 사람 확인이 필요하다.
- storage cleanup queue와 상태 추적은 구현됐지만, 실제 worker/service-role 재처리 경로는 아직 운영 환경에서 검증하지 못했다.
- `unknown_pending_confirmation`은 앱과 SQL 양쪽에 반영됐지만, 실제 timeout 재현 QA는 아직 남아 있다.
- `detectSessionInUrl: false` + custom scheme 조합은 의도된 계약이지만, Supabase dashboard redirect 허용과 메일 앱 복귀 실기기 QA는 아직 남아 있다.
- Task 4는 종료됐지만, release 직전 SQL/콘솔 캡처, restore/manual cleanup SOP, `qa-task4-*` 테스트 데이터 정리는 비차단 운영 보강 메모로 남는다.
- 댓글 author fallback은 앱 쪽 최소 보정으로 안정화했지만, 공개 post snapshot이 전혀 없는 신규 작성자까지 100% 보장하는 서버 계약은 아직 아니다.
- 커뮤니티 댓글/리스트/공통 header/typography 리팩터링은 1차 반영됐지만, 실기기 성능과 UX QA는 더 필요하다.
- 장소/여행 상세는 emulator 기준으로 `위치 미리보기`를 앱 안에서 직접 이동/확대/축소 가능한 지도 형태로 열었지만, 물리 실기기에서 스크롤 충돌 없이 같은 체감이 나는지 최종 캡처가 아직 없다.
- 여행 상세의 `더보기/접기`는 emulator 기준 재확인에서 정상 노출됐지만, 물리 실기기에서 같은 토글 노출과 스크롤 체감이 유지되는지는 아직 남아 있다.

### 미해결 / 다음 작업 대상

- 장소/여행은 public label 분리, trust read-only 연결, user layer read/write 분리, stale/conflict 강등 기준까지 반영됐지만 장기 trust 운영 경로와 admin review write path는 아직 남아 있다.
- Kakao Local만으로는 실제 반려동물 동반 가능 여부를 100% 보장할 수 없다.
- `confirmed`는 계속 닫혀 있고, admin review write path, 운영 증적, 실기기 QA 없이 개방 검토로 넘어가면 안 된다.
- Supabase 장소 메타와 여행 trust read-only, personal layer read/write, stale/conflict 런타임 적용까지는 연결됐지만, 운영 검수 write path와 release 직전 실기기 QA는 아직 없다.
- Task 5 physical-device QA는 release blocker 유지가 맞지만 외부 선행조건 문제라, 다음 engineering slice의 직접 목표로 다시 올리지 않는다.
- Task 7로 `커뮤니티 list preview fetch 최적화` 1차는 닫혔고, Task 8 조회수 최소 실서비스 계약도 이번 턴에 닫혔다.
- 커뮤니티 성능 2차는 preview fetch가 아직 post별 1회라는 잔여 리스크가 실제 체감 병목으로 남을 때만 batch contract 또는 추가 최적화를 검토한다.
- 외부 API 키/환경 분리/운영 도구 분리와 같은 장기 운영 인프라 리스크는 여전히 남아 있다.
- 최종 법무 문안, 정책 URL, 위치정보 고지, 운영정책 문서화는 아직 미완성이다.

## 최신 운영 방향

- 5차는 기능 확장보다 계약, 운영, 신뢰도 방어선 우선으로 진행한다.
- 현재 작업 순서는 `Task 4 종료 정리 -> Task 5 1차 최소 구현 -> User Layer 정합성 연결 -> stale/conflict 운영 기준 보강 -> Task 6 remote 정합성 재검증 -> Task 6 초기 운영 seed 입력 + 실차단 검증`까지 왔다.
- Task 4는 repo 기준 구현, remote blocker 해소, linked project 재검증까지 끝나 완료 상태로 유지한다.
- Task 4 완료 후 운영 메모는 `community_reporter_flags` follow-up RPC 의존, SQL/콘솔 SOP 중심 운영, restore/manual cleanup 절차 보강 권장, `qa-task4-*` 테스트 데이터 정리다.
- 최종 정책 문서 링크와 운영 고지가 확정되기 전에는 정책 없는 회원가입 UX 확장, 소셜 로그인 선구현, 결제 선구현, Guestbook 기능 확장, moderation 없는 커뮤니티 기능 확장을 하지 않는다.
- Task 1에서는 정책 본문을 새로 쓰지 않고 정책 연결 구조와 안전한 fallback만 정리했다.
- Task 2.5에서는 최종 법무 문안 확정 대신 초안 구조와 가입 UX를 먼저 정리했다.
- Task 2에서 삭제 계약은 구현까지 반영됐고, 계정 삭제는 `요청 수락`과 `최종 정리 완료`를 분리해 추적한다.
- storage cleanup은 DB와 분리된 queue 계약으로 다루고, pending 상태를 성공으로 포장하지 않는다.
- Task 3는 구현까지 반영됐고, Task 4는 PO lock-in 값 기준 최소 구현까지 반영됐다.
- Task 4 repo 검증에서는 `partial image upload orphan 회수`, `report 후 auto-hidden post 재동기화`, `release checklist의 운영 증적 분리`까지 닫혔다.
- Task 4 후속 점검에서는 댓글 작성자 unknown 이슈의 원인을 `profiles` 의존 + RLS 제약 + refetch overwrite로 확정했고, DB/RLS 변경 없이 앱 author fallback과 store merge만 최소 보정한다.
- Task 5는 이번 턴에 `문서/type source of truth 정리 -> trust read-only 연결 -> candidate/trust UI 분리 -> 후보 과다 노출 완화`까지 최소 구현했다.
- Task 5-5는 이번 턴에 `personal state read/write 연결 -> 공개 라벨과 개인 상태 UI 분리 -> recent search local화 + search log best-effort 기록 -> 로그아웃/계정삭제 정리`까지 반영했다.
- Task 5 후속 QA에서는 emulator 기준으로 place/travel 상세 지도 미리보기가 인터랙티브해지도록 최소 보정했고, 긴 설명은 3줄 + 더보기/접기 구조로 줄였다.
- 2026-03-26 release 전 QA 시도에서는 `adb devices -l` 기준 Android emulator만 연결돼 있었고, 물리 Android/iOS 기기 증적은 확보하지 못했다.
- 같은 시도에서 여행 상세 `더보기` 토글은 실제 화면 계층에서 다시 확인됐고, strict release 판정은 여전히 물리 실기기 QA와 운영 캡처가 남아 있는 상태로 유지한다.
- 같은 시도에서 linked project 운영 조회 경로는 `community_moderation_queue`, `community_moderation_actions`, `community_image_assets`, `community_reporter_flags`의 row count 수준까지는 다시 확인했지만, row-level dump/capture는 Docker 환경 제약으로 아직 닫히지 않았다.
- 이번 App-only 재판정 턴에서도 물리 Android/iOS 연결에 대한 새 증적은 추가되지 않았고, release blocker는 그대로 유지한다.
- 현재 사실은 `pet-friendly-place = candidate-first hybrid + trust read-only + personal layer + stale/conflict downgrade`, `pet travel = candidate-layer base + trust read-only + personal layer + stale/conflict downgrade`, `walk = candidate base + 보수 라벨 + local recent search`, `confirmed closed`다.
- 다음 우선순위는 `커뮤니티 list preview fetch 최적화 -> Task 5 physical-device QA / 운영 캡처 재점검 -> confirmed 개방 검토 여부 재판정` 순서로 잡는다.
- release 직전에는 community moderation/cleanup SQL 콘솔 절차 캡처를 병렬로 보강한다.
- 비밀번호 재설정은 `Supabase recovery link + custom scheme 앱 복귀 + 새 비밀번호 설정 화면 + 성공 후 SignIn 재로그인 유도`로 고정한다.
- reset 요청 단계의 사용자 메시지는 계정 존재 여부를 노출하지 않는 보수 문구를 유지하고, 앱 레벨 재발송 제한은 60초 쿨다운으로 고정했다.
- reset 링크 만료/오류 화면은 SignIn보다 reset request 화면 재진입을 우선 제공한다.
- 전역 typography source of truth는 `src/app/theme/tokens/typography.ts`와 `src/app/ui/AppText.tsx`를 기준으로 `display / titleLg / titleMd / titleSm / body / bodySm / button / tab / helper / caption` 체계로 정리했다.
- 기존 `AppText` preset alias는 호환성을 유지하되 `title1 -> display`, `title2 -> titleLg`, `headline -> titleSm`, `body -> 16/24`, `caption -> helper(13/18)` 기준으로 상향했다.
- 공통 접근성 보정 1차 범위는 하단 탭, 헤더 액션 버튼, Toast, ConfirmDialog, Auth 스타일, GuestHome, Community 카드/리스트/상세다.
- 하단 탭 라벨 `9/11`, 헤더 액션 `34~36`, Community/Auth의 다수 `10~12px` 텍스트를 제거했고, 주요 액션은 `44~48` 터치 영역 기준으로 올렸다.
- 이번 리팩토링은 typography/accessibility 범위로 제한했고 기능 계약, DB/Auth/Supabase 계약 변경은 포함하지 않았다.
- 실기기 기준 추가 확인이 필요한 축은 `200% 글꼴 확대`, `긴 한글 CTA 2줄`, `CommunityDetail 댓글 입력/신고 시트`, `GuestHome 카드형 레이아웃`이다.
- 위치 기반 기능은 복잡한 지역 브라우징 대신 `현재 GPS 기반 주변 추천 + 직접 검색`으로 고정한다.
- 펫동반 장소 탐색은 단기적으로 외부 API 결과를 쓰되, 중기적으로는 `외부 원본 + Supabase 메타 혼합`으로 간다.
- 반려동물과 여행은 GPS 필수 도메인으로 보지 않고 `검색 입력 + 카테고리` 기반 여행 탐색으로 정리한다.
- 펫동반 검증 상태는 `unknown / keyword-inferred / user-reported / admin-verified / rejected`로 분리하고, 카카오 결과만으로 확정 표시하지 않는다.
- Supabase 장소 메타는 `canonical meta + source link + external signal` 구조로 설계하고, 상태 제약은 초기에는 `text + check constraint`를 우선 검토한다.
- 반려동물과 여행 펫정책은 `Raw Layer -> Candidate Layer -> Trust Layer -> Aggregation Layer` 4단계 구조로 정리한다.
- `confirmed`는 향후 `pet_policies + user_reports + admin review` 조합이 붙었을 때만 허용한다.
- 이번 단계의 Trust Layer는 `pet_travel_places / pet_travel_pet_policies / pet_travel_user_reports` 3테이블로 시작하고, `source_links`는 후속 확장으로 미룬다.

## 현재 source of truth

- 행동 규칙
  - `docs/engineering/advanced-codex-workflow.md`
  - `docs/engineering/advanced-codex-checklist.md`
  - `docs/engineering/advanced-codex-memory.md`
- 5차 총괄 문서
  - `docs/프로젝트-현황-최종기획/5차-최종-기획서.md`
- project-memory
  - `docs/project-memory/현재-프로젝트-상태.md`
  - `docs/project-memory/핵심-결정사항.md`
  - `docs/project-memory/최근-작업-로그.md`
  - `docs/project-memory/다음-작업-우선순위.md`
- 정책 연결 구조
  - `src/services/legal/documents.ts`
  - `src/services/legal/consents.ts`
  - `docs/policies/이용약관-초안.md`
  - `docs/policies/개인정보처리방침-초안.md`
  - `docs/policies/계정-삭제-안내-초안.md`
- 계정 삭제 현재 호출 경로
  - `src/screens/More/MoreDrawerContent.tsx`
  - `src/screens/More/MoreScreen.tsx`
  - `src/services/auth/session.ts`
  - `src/services/supabase/auth.ts`
- 계정 삭제/동의 SQL 기준
  - `docs/sql/인증-계정/계정삭제-동의이력-최종.sql`
- release QA 기준
  - `docs/qa/release-checklist.md`
- Task 4 구현 근거 코드 / SQL
  - `src/services/supabase/community.ts`
  - `src/services/supabase/storageCommunity.ts`
  - `src/screens/Community/CommunityCreateScreen.tsx`
  - `src/screens/Community/CommunityDetailScreen.tsx`
  - `src/screens/Community/communityPostSubmit.shared.ts`
  - `src/types/community.ts`
  - `docs/sql/커뮤니티/커뮤니티-운영-최종.sql`
  - `docs/qa/release-checklist.md`
- remote 커뮤니티 운영 source of truth 재검증
  - linked project: `grmekesqoydylqmyvfke`
  - 확인 경로: `supabase projects list`, `supabase db dump --linked --dry-run`, `python3 + psycopg`로 `pg_proc / pg_trigger / pg_policies / pg_constraint / pg_attribute / pg_class` 조회, service-role REST HEAD로 `blocked_nicknames / blocked_nickname_patterns / posts / comments` count와 `posts?select=view_count` 오류 재확인
- Task 3 구현 근거 코드
  - `src/screens/Auth/SignInScreen.tsx`
  - `src/screens/Auth/PasswordResetRequestScreen.tsx`
  - `src/screens/Auth/PasswordResetRecoveryScreen.tsx`
  - `src/screens/Auth/PasswordResetFormScreen.tsx`
  - `src/screens/Auth/PasswordResetFlow.styles.ts`
  - `src/services/supabase/auth.ts`
  - `src/services/supabase/account.ts`
  - `src/services/supabase/client.ts`
  - `src/services/app/boot.ts`
  - `src/app/providers/AppProviders.tsx`
  - `src/navigation/RootNavigator.tsx`
  - `src/navigation/linking.ts`
  - `App.tsx`
  - `android/app/src/main/AndroidManifest.xml`
  - `ios/nuri/Info.plist`
  - `ios/nuri/AppDelegate.swift`
- 위치 freshness 정책: `src/services/location/currentPosition.ts`
- 위치 기반 탐색 구조: `src/services/locationDiscovery/*`
- 산책 정책 문서: `docs/domains/산책-조회-신뢰성-개선/산책-조회-신뢰성-개선-통합-완성본.md`
- 펫동반 장소 탐색 문서: `docs/domains/펫동반-장소-탐색/펫동반-장소-탐색-구현-상세.md`
- 펫동반 장소 메타 SQL: `docs/sql/펫동반-장소/펫동반장소-메타-최종.sql`
- 펫동반 신뢰도 보강 API 전략: `docs/domains/펫동반-장소-탐색/펫동반-장소-탐색-신뢰도-보강-API-전략.md`
- 반려동물과 여행 설계: `docs/domains/반려동물과-여행/반려동물과-여행-초기-설계.md`
- 반려동물과 여행 펫정책 아키텍처: `docs/domains/반려동물과-여행/반려동물과-여행-펫정책-아키텍처.md`
- 반려동물과 여행 Trust Layer 스키마 초안: `docs/domains/반려동물과-여행/데이터베이스/반려동물과-여행-Trust-Layer-스키마-초안.md`
- 반려동물과 여행 Trust Layer SQL: `docs/sql/반려동물과-여행/반려동물과여행-트러스트레이어-최종.sql`
- 4차 총괄 점검 문서: `docs/프로젝트-현황-최종기획/4차-최종-기획-진행서.md`
- 4차 장기 운영 리스크 문서: `docs/프로젝트-현황-최종기획/4차-우려사항-및-장기운영-리스크.md` (과거 리스크 기준 + 현재 상태 표기)

## Android 시스템 바 상태

- Android 시스템 하단 내비게이션 바는 `styles.xml`의 `navigationBarColor/windowNavigationBarColor = #FFFFFF`와 `MainActivity.kt` 런타임 재적용 기준을 함께 사용한다.
- Android 시스템 하단 내비게이션 바는 `styles.xml`의 `navigationBarColor = #FFFFFF`와 `MainActivity.kt` 런타임 재적용 기준을 함께 사용한다.
- Android 15+에서는 `values-v35/styles.xml`의 `windowOptOutEdgeToEdgeEnforcement = true`를 추가로 사용한다.
- `MainActivity.kt`는 `onCreate/onResume/onWindowFocusChanged`에서 시스템 바 스타일을 재적용한다.
- 상태바는 기존 투명 정책을 유지하고, 이번 변경은 하단 시스템 바에만 적용했다.
- 실기기 기준으로 현재 하단 시스템 내비게이션 바는 흰색으로 정상 복구된 상태다.
